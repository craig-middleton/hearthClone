# HearthstoneClone - Project Status

## Working Preferences
- Craig wants a 2-paragraph explanation after each new/updated code block, describing what it does and why.
- For code changes, always provide the full rewritten class/file, not just a diff/snippet — Craig pastes a complete file each time rather than manually merging partial additions.
- For hearthClone architecture, avoid singletons where possible — prefer explicit constructor/field-passed references (as the project already does throughout) over globally-reachable static instances, to keep dependencies visible and avoid Unity singleton pitfalls (script execution order, DontDestroyOnLoad issues, creeping responsibility).
- **Clarified session 27**: the no-singletons principle is about avoiding *global static state*, not about avoiding parameters. Passing a dependency like `Board` explicitly into a method is exactly the preferred style — this was initially misread during the Taunt refactor and briefly led to a weaker design (see Session 27 notes, finding B1).

_Last updated: 2026-08-16 (session 27 — playtested and confirmed all 4 of session 26's bug fixes; completed the Taunt refactor centralising the rule inside `Combat.TryAttack`; completed the null/`IsDead` guard backfill across all five view scripts; a Claude Code review of both changes surfaced 10 findings, all fixed in-session, then a Claude Code follow-up pass closed the finding-D remainder and compile-verified all five assemblies)_

## Tooling note: Claude Code now in the workflow
Craig has Claude Desktop installed on Ubuntu and has started using its **Code** tab (Local environment, pointed at the `hearthClone` project folder) as an additional way to work on the codebase, alongside this chat-based workflow. Claude Code reads/edits files directly on disk — no more copy-pasting needed when working that way. Both workflows are expected to continue being used; this file should stay the single source of truth regardless of which one makes a given change, so any session (chat-based or Claude Code) should update it. **Discipline that keeps this safe**: whoever is driving (this chat or Claude Code) should paste/read the *actual current* file before editing it, never assume memory of a file's contents is accurate — this already caught real problems (missing `avatarImage` field, background/music code not actually saved) and is the main safeguard against the two tools silently drifting out of sync with each other.

**Session 27 refined the division of labour**: the chat workflow wrote the refactor, then Claude Code reviewed it with a prompt describing exactly what changed and what to check. That caught a behaviour regression the chat workflow had introduced and missed (see finding A1). Worth repeating as a pattern — write in one tool, review in the other, with the review prompt scoped to the actual diff rather than asking for another full-codebase sweep.

**Tooling discovery (session 27)**: Claude Code can compile-verify changes without opening Unity. `dotnet build Core.csproj` — and the same for `Effects`, `Cards`, `AI`, `UI` — works against the Unity-generated `.csproj` files and resolves the engine/TMPro references correctly. All five currently build clean, so that's a usable baseline. This does *not* replace playtesting (it catches compile errors, not behaviour), but it means a Claude Code session can make multi-file edits and confirm the project still builds before handing back, instead of leaving Craig to discover a syntax error on next Play.

**Also learned session 27**: pasting this file into a fresh chat can silently truncate its middle section, and the chat may not realise content is missing. It answered "that card isn't in your pool" about `TestCard_MagePupil`, which *is* documented here at the Test Assets section — it just fell inside the truncated range. If a session claims something documented here doesn't exist, suspect truncation before believing it.

## How to use this file
Paste the contents of this file at the start of any new Claude chat to get instant context on the project. Update it at the end of each working session (ask Claude to update it, or do it yourself) so it never goes stale.

## Card Artwork — DONE (earlier session)
Full pipeline built and verified working end-to-end:
- **Art source**: Leonardo AI (free tier), JPG downloads work fine (no transparency needed since art fills the card's background area). Aspect ratio guidance used: portrait, close to 3:4, e.g. 768×1024 or similar.
- **Import**: drag JPG into `Art` folder → select it → Inspector → **Texture Type** → `Sprite (2D and UI)` → Apply.
- **Assignment**: drag the imported sprite onto the relevant `CardData` asset's pre-existing `Artwork` field (this field existed since the very first `CardData.cs` but was unused until now).
- **Code (both `CardView.cs` and `MinionView.cs`)**: each gained a new `Image artworkImage` field; the existing shared text-writing helper (`WriteCardText()` in `CardView`, inline in `MinionView.SetMinion()`) now also sets `artworkImage.sprite` when `card.artwork`/`minion.Artwork` is non-null, or sets `artworkImage.enabled = false` to cleanly hide the image slot when there's no art — so cards/minions without generated art still render correctly as text-only, no broken/blank sprites.
- **`Minion.cs`**: gained an `Artwork` field (`Sprite`) and a new optional constructor parameter, since board minions are runtime objects separate from `CardData` and needed their own copy of the reference. `PlayerHand.PlayCard()` passes `card.artwork` through when constructing a `Minion`, mirroring how `card.hasTaunt` was already threaded through the same constructor call.
- **Editor setup (done for both `CardView` and `MinionView` prefabs)**: added a new `Image` child (`ArtworkImage`) positioned as the *first* child (so it renders behind the name/stats text, not covering it), wired to the new script field.
- **Bug hit and fixed in that session**: on `MinionView`, the `Artwork Image` field was left unassigned (`None`) after adding the child — this is the same "empty field fails silently" pattern hit several times before (Button/Image references need explicit dragging, the code can't infer it). Board minions showed a blank white box (Unity's default unconfigured `Image` color) instead of either the real art or a clean hidden fallback, until the field was actually wired.
- **Confirmed via screenshot**: Goblin (the only card with generated art so far) displays correctly on both the hand card and the board minion, on both players' sides; cards/minions without art (Wisp, Shieldbearer, etc.) correctly show as plain text with no visual glitch.
- **Only one card has real art so far (Goblin)** — the rest of the 15-card pool still needs art generated the same way if Craig wants full coverage. No further code/Editor work needed for additional cards — just repeat the generate → import → assign steps per card. Still true as of session 27.

## Board Background & Music — DONE (earlier session)
Full pipeline built and verified working end-to-end, following the same "list + random pick" shape as `cardPool`:
- **Background art source**: same tools as card art (Leonardo AI etc.), but **landscape/wide orientation** this time (e.g. 1536×1024 or 1216×832), prompted for a framed "game board" feel (bordered edges, empty foreground for game pieces, no text) — 3 backgrounds generated and imported. Style prompts used: cartoon/painted fantasy, warm saturated colors, whimsical (deliberately generic/genre-level rather than referencing any specific existing game's exact visual identity).
- **Music source**: AI music generators — Mubert (ambient/background specialist), AIVA (structured orchestral/game-soundtrack), Sonauto (unrestricted free tier) all viable free options. Prompt used: whimsical fantasy tavern instrumental, no vocals, seamless loop, upbeat but not distracting.
- **Scene setup**: `BoardBackground` — a new full-screen `Image` child under `GameCanvas`, positioned as the **first** child (renders behind everything), Rect Transform stretched to fill (`Alt+Shift` + stretch-both anchor preset), **Raycast Target unchecked** proactively (learned from the earlier `BoardPanel` click-blocking bug — a background should never intercept clicks). An `Audio Source` component added directly onto the `EffectTester` GameObject, with `Play On Awake` and `Loop`... **`Loop` is set by the script itself** (`musicSource.loop = true` in `SetRandomMusic()`), so only `Play On Awake` needs manually unchecking in the Inspector (playback is script-controlled, not automatic).
- **Code (`EffectTester.cs`)**: `boardBackgroundImage`/`boardBackgrounds` and `musicSource`/`musicTracks`/`musicVolume` fields, `SetRandomBoardBackground()` and `SetRandomMusic()` methods, both called at the very top of `Start()`.
- **Bug hit and fixed in that session**: the background wasn't varying between Play-mode restarts — root cause was Unity's default `Random` seeding, which can produce the same first result on rapid consecutive Play-mode restarts within the Editor (a known Unity quirk, not a real logic bug). Fixed with an explicit `Random.InitState(System.DateTime.Now.Millisecond + System.Environment.TickCount)` call at the top of `Start()`, shared by both the background and music picks. A debug log was added to `SetRandomBoardBackground()` showing the picked index/sprite name, to make this kind of issue directly verifiable in the console rather than relying on eyeballing a visual that might look similar between two different picks.
- **Confirmed via screenshot**: full game scene rendering with a proper bordered fantasy-arena-style background behind all UI, cards/boards/panels all correctly layered on top and still fully clickable (Raycast Target fix held). Still working normally through session 27's playtesting.

## Project Goal
A Hearthstone-style card game built in Unity, single-player vs AI, using C# with a data-driven card/effect system (ScriptableObjects). Developed on Ubuntu using Unity + VS Code, version controlled via Git/GitHub (with Git LFS enabled for art/audio).

**Unity version: 6.5 (60000.5.2f1).** Use TextMeshPro (TMP) for all UI text, not legacy `UnityEngine.UI.Text`.

## Tooling Setup (Done)
- Unity Hub + Unity LTS installed via AppImage
- VS Code with C# Dev Kit + Unity extension
- .NET SDK installed (for VS Code C# debugging, separate from Unity's own runtime)
- Git + GitHub connected via Unity Hub's built-in integration, authenticated via `gh auth login`
- Git LFS enabled for binary assets (art, audio)
- Repo: `github.com/craig-middleton/hearthClone`

## Architecture Decisions
- **Folder structure** under `Assets/_Project/`: Scripts, ScriptableObjects, Prefabs, Art, Scenes
- **Card/effect model**: data-driven via ScriptableObjects — cards reference reusable `CardEffect` assets instead of each card having bespoke code.
- **Assembly Definitions** (`.asmdef`) enforce one-way dependency flow: `Core` (no deps) ← `Effects` (deps: Core) ← `Cards` (deps: Core, Effects) ← `AI` (deps: Core, Cards, Effects) ← `UI` (deps: Core, Cards, Effects, **AI**). `Effects` must NOT reference `Cards`. **Corrected session 26** — this was previously (mis)documented as `AI`/`UI` being parallel leaves; in reality `UI` also references `AI` directly (`EffectTester` constructs and calls `AIController`), confirmed via both the asmdef reference list and the `using HearthstoneClone.AI;` in `EffectTester.cs`. Not circular, compiles fine — just was inaccurately described here previously.
- **No singletons** — every class is constructed explicitly and handed the exact references it needs. **Note (session 27)**: this does not mean avoiding parameters. `Combat.TryAttack` now takes `Board` and the defending `Player` as explicit arguments, which is the same principle applied at method level rather than constructor level.
- **Rules live at the chokepoint, not at the call sites** (established session 27). If every path to an action funnels through one method, the rule governing that action belongs *inside* that method, not duplicated at each caller. See the Taunt refactor below for the worked example.

## Code Written So Far

| File | Location | Purpose |
|---|---|---|
| `CardEffect.cs` | `Scripts/Effects/` | Abstract `ScriptableObject` base; `Execute(GameContext, Target)`. |
| `DealDamageEffect.cs` | `Scripts/Effects/` | Deals damage to a Target, logs remaining health. |
| `GainManaEffect.cs` | `Scripts/Effects/` | Calls `target.GainMana(manaAmount)`. Powers The Coin. |
| `CardData.cs` | `Scripts/Cards/` | ScriptableObject: `cardName`, `description`, `artwork` (Sprite — now actively used, see Card Artwork section), `manaCost`, `cardType`, `attack`, `health`, `hasTaunt`, `onPlayEffect`, `targetsSelf`. |
| `Minion.cs` | `Scripts/Core/` | Runtime board minion. `HasSummoningSickness` (true from construction), `HasAttackedThisTurn`, `CanAttack` (computed), `ResetForNewTurn()`, `HasTaunt` (optional constructor param, `false` default), `Artwork` (Sprite, optional constructor param, `null` default). All new fields use optional constructor parameters so existing call sites keep compiling unchanged as each was added. **Changed session 27**: `CanAttack` now includes `!IsDead` — previously dead-ness and attack-eligibility were entirely independent, so a corpse that survived to render would still report itself as able to attack, and both `MinionView`'s eligibility tint and `EffectTester`'s selection check read `CanAttack` directly. Strictly narrowing change — nothing that returned `false` before returns `true` now. |
| `Player.cs` | `Scripts/Core/` | Health, mana (current/max), board minions list, `FatigueDamage`, `HasUsedHeroPowerThisTurn`. No `IsAI` flag. |
| `Board.cs` | `Scripts/Core/` | Holds both `Player`s; `GetOpponent()`, `RemoveDeadMinions()`, `GetTauntMinions(Player player)` (returns that player's Taunt minions — no separate "alive" filter needed since dead minions are already stripped by `RemoveDeadMinions()`). **Session 27**: `GetTauntMinions()` is now called from exactly one place — inside `Combat.TryAttack` — rather than from three separate call sites. |
| `GameContext.cs` | `Scripts/Core/` | Holds a real `Board` reference. |
| `Target.cs` | `Scripts/Core/` | Points to a `Player` or `Minion`. `TakeDamage()`, `GetCurrentHealth()`, `GainMana(int)`. Note: a `Target` constructed with a null argument yields a non-null object with *both* fields null — `Combat.TryAttack` now explicitly rejects that case (session 27, finding A5). |
| `Combat.cs` | `Scripts/Core/` | Static class. **Signature changed session 27** to `TryAttack(Minion attacker, Target target, Board board, Player defender, out string failReason)`. Now the single owner of the Taunt rule — it calls `board.GetTauntMinions(defender)` itself, so no caller can bypass, forget, or misconfigure it. Guard order is deliberate: null `attacker`/`target` → null `board`/`defender` → malformed `Target` (both fields null) → attacker `IsDead` → target minion `IsDead` → Taunt → `CanAttack`. Taunt sits *above* `CanAttack` to preserve the pre-refactor failure-message precedence; the dead-attacker check sits above both because `Minion.CanAttack` now includes `!IsDead` and would otherwise report a misleading "summoning sickness"/"already attacked" reason for a corpse. Applies damage to the target and strikes back at the attacker if the target is a minion (simultaneous damage). Still static/stateless — a pure function, not a singleton. |
| `TurnManager.cs` | `Scripts/Core/` | Turn order + mana progression. A private `StartTurnFor(player)` wraps mana refill, `ResetMinionsForNewTurn(player)` (clears summoning sickness/attacked flags), and `player.HasUsedHeroPowerThisTurn = false` — called from both `StartGame()` and `EndTurn()`. Guarantees a minion summoned on one turn stays sick through the opponent's whole turn, clearing only on the controller's own next turn — matching real Hearthstone timing with no separate counter needed. |
| `EffectTester.cs` | `Scripts/UI/` | Bootstrapper. Builds decks/hands, asymmetric opening hands + Coin, full mulligan flow, manual control mode toggle, click-to-attack combat wiring, `FaceView` health/mana display, win condition, per-turn draw, Taunt enforcement, minimal Hero Power, random board background + music (`boardBackgroundImage`/`boardBackgrounds`, `musicSource`/`musicTracks`/`musicVolume`, both picked via a shared `Random.InitState(...)` seed at the top of `Start()`). **Session 26 — four real bugs found via Claude Code review, all fixed**: (1) `OnCardClicked()`/`OnOpponentCardClicked()` never called `CheckWinCondition()` on a successful play — a lethal spell let the game continue instead of ending. (2) `SetRandomBoardBackground()`/`SetRandomMusic()` were called *after* the `cardPool` empty-check `return`, producing a silent black screen with no audio instead of a visible board with a warning; both moved to the top of `Start()`. (3) `endTurnButton`'s click listener was registered *inside* `OnConfirmMulliganClicked()`, and `ConfirmMulliganButton` remained active and clickable for the entire match — clicking it twice registered the listener twice, silently doubling every subsequent turn. Fixed by registering once in `Start()`, adding a `mulliganComplete` guard, and deactivating the button after use. **Session 27 changes**: `ResolveAttack(Minion, Target, Player defender)` now passes `board` + `defender` to the new `Combat.TryAttack` signature, and clears `selectedAttacker` **only on success** (see finding A1). New private `AfterGameAction()` helper bundles `RemoveDeadMinions()` + `RefreshAll()` + `CheckWinCondition()` and is now called by *every* state-changing path (card plays, hero power, attacks, end turn) — previously only the two attack paths swept corpses. `OnMinionClicked`/`OnFaceClicked` shed their duplicated Taunt checks entirely. `ShowMulliganUI()` and `BuildDeck()` skip null cards, and `ShowMulliganUI()` validates the `CardView` component before wiring anything (previously a guard could fire *after* `Instantiate` had already parented a blank ghost card into the panel). **Not permanent** — delete/rename once real gameplay loop exists. |
| `PlayerHand.cs` | `Scripts/Cards/` | Wraps a `Core.Player` with `Deck`/`Hand`. `Shuffle()`, `DrawOpeningHand()`, `AddCardToHand()`, `MulliganCard()`. `DrawCard()` deals escalating fatigue damage when the deck is empty; burns a newly-drawn card if hand is full (10). `PlayCard()` blocks Minion plays if the board is full (7, checked before mana/hand are touched), and passes `card.hasTaunt`/`card.artwork` through to the `Minion` constructor on summon. **Bug fixed via Claude Code (session 23)**: the effect-execution line silently skipped a card's effect with no log if a target wasn't provided — now logs a warning instead. Taunt and artwork fully verified working; fatigue/board cap not yet stress-tested (need a long enough game to trigger either). Note: this is the **only** place `new Minion(...)` appears in the codebase — see the reference-identity note in Lessons Learned. |
| `CardView.cs` | `Scripts/UI/` | Displays one card. Two setup modes sharing a private `WriteCardText()` helper — `SetCard()` (play mode) and `SetCardForMulligan()` (toggle-select mode with dim/highlight visual). Has an `artworkImage` field; `WriteCardText()` sets `artworkImage.sprite = card.artwork` when present, or `artworkImage.enabled = false` to cleanly hide it when a card has no art — works for both display modes since both share the helper. **Session 27**: both `SetCard()` and `SetCardForMulligan()` now guard their `cardData` argument for null and return early with a warning. |
| `HandDisplay.cs` | `Scripts/UI/` | `RenderHand(List<CardData>, Action<CardData>)` — generic, reused for both hands. **Session 27**: returns early if the list is null, and skips null entries rather than instantiating a broken view. Deliberately does *not* log per-entry — a null inside a list iterated on every refresh would spam the console; the view scripts log once per bad call instead, which is the more useful signal. |
| `MinionView.cs` | `Scripts/UI/` | Displays one board minion. Clickable (`Button`), `minionBackground` (Image) for selected/attack-eligibility tinting, `nameText` appends `" (Taunt)"` for Taunt minions. `SetMinion()` takes optional `clickCallback`, `isSelected`, `showAttackEligibility` (all defaulted so old call sites still compile). Has an `artworkImage` field, same pattern as `CardView`. **Bug hit and fixed in an earlier session**: the `Artwork Image` field was left unassigned in the prefab Inspector after adding the child, causing a blank white box instead of either art or a clean hidden fallback. **Session 27**: `SetMinion()` now guards its `minionData` argument for null and returns early with a warning. |
| `BoardDisplay.cs` | `Scripts/UI/` | `RenderBoard()` — takes optional `onMinionClicked`, `selectedAttacker`, `showAttackEligibility`, passed straight through to each `MinionView.SetMinion()` call. Generic, reused for both boards via two separate instances. **Session 27**: guards `boardPanel` before the child-clearing loop (which would otherwise NRE on an unassigned panel *before* any argument guard was reached), guards `minionViewPrefab`, skips null minions, **skips `IsDead` minions** as defence-in-depth against a future damage path forgetting to sweep corpses, and destroys an instantiated object whose `GetComponent<MinionView>()` comes back null rather than leaving a blank ghost parented in the layout. |
| `FaceView.cs` | `Scripts/UI/` | The visible player health + mana display. `SetPlayer(Player, Action<Player> clickCallback)` writes `"{PlayerName}: {Health} HP\nMana: {CurrentMana}/{MaxMana}"` and wires a `Button` click to the callback — doubles as the attack target for face damage. Has an `avatarImage` field for a static hero portrait (assigned once, no runtime lookup needed since it doesn't change mid-game); a procedural idle animation in `Update()` — sine-wave "breathing" scale pulse + slow horizontal sway, base scale/position captured on first `SetPlayer()` call, four tunable fields. Confirmed working: both hero portraits (Warrior for Player One, Spellcaster for Player Two) rendering next to their health/mana displays. **Session 27**: `SetPlayer()` now guards its `playerData` argument for null. This file was *missed* by the initial guard backfill (it wasn't on the Next Steps list, which named only the other four view scripts) and was caught by the Claude Code review — it guarded its serialized fields but not its argument, and without the guard the button closure would capture a null `player` and silently pass it into `OnFaceClicked` on click. **Still not addressed**: base position/scale capture happens potentially before the first Canvas layout pass — if it lands pre-layout, the avatar could end up permanently offset with no error. Also, the two avatar `Image`s are set up inconsistently in the scene (one stretch-anchored, one point-anchored), and both currently have `Raycast Target` checked (should be unchecked per the established habit for decorative images). |
| `AIController.cs` | `Scripts/AI/` | `PerformMulligan()`, `TakeTurn()` (card-play loop + attack phase). Attack loop checks `board.GetTauntMinions(opponent)` before choosing a target — attacks the first Taunt minion found if any exist, otherwise face. **Bug found and fixed session 26**: `board.RemoveDeadMinions()` previously ran only once, after the entire attack loop finished — meaning if the AI's first attacker killed a Taunt minion, it remained in `BoardMinions` for the rest of the loop, so subsequent attackers would target and "attack" the corpse. Fixed by moving `RemoveDeadMinions()` inside the loop. Also added an early `break` if the opponent's `Health <= 0`. **Session 27**: `TryAttack` call updated to the new signature (`board`, `opponent`); added `board.RemoveDeadMinions()` after the *card-play* loop too (it only swept during the attack loop before); null skips on cards and minions in both loops. The AI keeps its own targeting *heuristic* here (Taunt first, then face) — that's a strategy decision that legitimately belongs in the AI — but the *legality* of whatever it picks is now validated centrally. Note the centralised check is currently a permanent no-op on the AI path, since both heuristic branches pass by construction; it's insurance for when the heuristic gets more complex, not something that should fire today. |

## Test Assets Created / In Progress
- Original 5: `TestCard_Wisp` (1, 1/1), `TestCard_Goblin` (2, 2/2), `TestCard_RiverCroc` "River Crocodile" (3, 2/3), `TestCard_Fireball` (4, Spell → `Effect_Deal3Damage`), `TestCard_Boulderfist` (5, 4/4).
- `TestCard_Coin` "The Coin" (0, Spell, `targetsSelf = true` → `Effect_GainMana1`) — kept OUT of `Card Pool`, granted only via `EffectTester`'s dedicated `Coin Card` field.
- `Effect_Deal3Damage` (damage = 3), `Effect_GainMana1` (manaAmount = 1).
- **Completed in an earlier session** — 10 more unique cards created, bringing `Card Pool` to 15 total entries (confirmed via Inspector screenshot) × `Copies Per Card = 2` = a real 30-card deck, matching Hearthstone's max-2-copies deckbuilding rule rather than just raising the copy count on the original 5:
  - `TestCard_Murloc` (1, 1/1)
  - `TesCard_Watchman` (2, 3/1) — **note the typo in the asset name** ("Tes" not "Test"), purely cosmetic, doesn't affect function, left as-is at Craig's discretion to fix later
  - `TestCard_Shieldbearer` (3, 1/5)
  - `TestCard_Warhorse` (3, 3/3)
  - `TestCard_ArcaneBolt` (3, Spell, `targetsSelf = false` → `Effect_Deal3Damage`)
  - `TestCard_Bear` (4, 3/6)
  - `TestCard_MagePupil` (4, Spell, `targetsSelf = true` → `Effect_GainMana1`)
  - `TestCard_ChargingRhino` (6, 5/5)
  - `TestCard_StoneGuardian` (6, 4/8)
  - `TestCard_AncientColossus` (7, 7/7)
  - No code changes were needed for this — `BuildDeck()`/`Copies Per Card` already handled any pool size. Playtested extensively across sessions 26–27 with no pool-related errors.

## Verified Working
- **Board visuals** (session 9), **AI turn logic** (session 10), **opponent board visualization** (session 11), **shuffled decks + draw + opponent hand display** (session 12), **asymmetric opening hands + Coin + multi-card AI turn** (session 13), **full mulligan system, both sides** (session 14) — all previously confirmed.
- **Manual control mode**: toggle exists and Player Two's hand does become clickable when enabled. **Turn-gating bug found via direct testing** (Craig played Player Two's Coin during Player One's turn) — root cause diagnosed (missing `CurrentPlayer` check) and fix written, but **still not re-tested after the fix was applied** — confirm at some point that turn order is properly enforced in manual mode.
- **Session 27 playtest — all four of session 26's fixes confirmed working:**
  1. **Lethal via spell correctly ends the game.** Console showed `Player One played Arcane Bolt` → `Dealt 3 damage to target. Remaining health: -2` → `*** Player One wins! ***`, all in the same timestamp. Player Two's face displayed `-2 HP`. Additionally confirmed that **all input is genuinely locked out post-win** — no buttons respond, so the `gameOver` guards are working, not just the log line firing.
  2. **AI correctly re-evaluates targets after killing a Taunt minion** rather than continuing to attack the corpse. This closes the long-standing gap where AI-side Taunt enforcement had never been confirmed in play (see the Taunt section below).
  3. **`ConfirmMulliganButton` disappears after one press** and does not linger as an invisible-but-clickable element.
  4. **Background/music continued working normally** throughout all playtesting, with no console errors.

## Current Blocker / Last Thing Worked On

**Session 27 (this session)** — three pieces of work, in order:

### 1. Playtest of session 26's fixes — all 4 passed
See the Verified Working section above for detail. Nothing regressed; all four fixes held.

### 2. Taunt refactor — DONE
The rule was previously duplicated across three call sites (`EffectTester.OnMinionClicked`, `EffectTester.OnFaceClicked`, `AIController.TakeTurn`), while `Combat.TryAttack` — the one chokepoint every attack passes through — had no idea Taunt existed. It now lives solely inside `Combat.TryAttack`, whose signature changed to:

```csharp
TryAttack(Minion attacker, Target target, Board board, Player defender, out string failReason)
```

An **intermediate version passed a `List<Minion> defenderTaunts` parameter instead**, chosen to keep `Core.Combat` decoupled from `Board`. That was abandoned during review: a `null` list would have silently disabled the Taunt rule entirely — the exact silent-failure mode the refactor existed to eliminate, just relocated. Passing `Board` + the defending `Player` and letting `TryAttack` call `GetTauntMinions` itself closes the hole properly, and as a bonus restores the owner-named failure message (`Combat` now has a `Player` reference to name). `Board` lives in `Core` alongside `Combat`, so there was never an assembly obstacle — the original objection was a misreading of the no-singletons preference (see Working Preferences note).

### 3. Null/`IsDead` guard backfill — DONE
Argument null guards added to `MinionView.SetMinion`, `CardView.SetCard`, `CardView.SetCardForMulligan`, `FaceView.SetPlayer`, `BoardDisplay.RenderBoard`, `HandDisplay.RenderHand`; plus serialized-field and `GetComponent`-result guards in the two display scripts. `Combat.TryAttack` gained null, malformed-`Target`, and `IsDead` checks. `Minion.CanAttack` now includes `!IsDead`.

### Claude Code review of items 2 and 3 — 10 findings, all fixed in-session
- **A1 (regression introduced by the refactor itself):** moving the Taunt check into `ResolveAttack` meant a *rejected* attack now cleared `selectedAttacker`, which the old code preserved — so misclicking into a Taunt cost you your selection. Fixed by moving `selectedAttacker = null` inside the success branch only. Worth noting this was a real behaviour change the chat workflow made silently and didn't catch; the review did.
- **A2 (the load-bearing one):** only the two *attack* paths called `RemoveDeadMinions()` before refreshing — card plays, hero power, and the AI's card-play loop did not. Currently unreachable (spells are face-only), but it breaks the moment minion-targeting spells land, which is the very next planned feature. Fixed at the root with `EffectTester.AfterGameAction()` (sweep + refresh + win check) called by every state-changing path, plus `RemoveDeadMinions()` after `AIController`'s card-play loop, plus an `IsDead` skip in `BoardDisplay.RenderBoard` as defence-in-depth. Without this, a spell-killed minion would have rendered as a live `2 / -1` card and stayed clickable.
- **A3:** `FaceView.SetPlayer` was missed by the backfill entirely — it wasn't on the Next Steps list, which named only four view scripts. Fixed.
- **A4:** Taunt was being checked *after* `CanAttack`, reversing the old precedence (an exhausted attacker clicking past a Taunt got "already attacked this turn" instead of the Taunt message), and the message had lost the owner's name. Both restored.
- **A5:** a `Target` with both fields null passed every guard, no-opped on damage, and still set `HasAttackedThisTurn = true` — burning the attack and returning `true`. Now explicitly rejected.
- **B1:** the `List<Minion>` parameter design — resolved by switching to `Board`/`Player`, see above.
- **B2 (noted, no fix needed):** the centralised Taunt check can never fire on the AI path, since the AI's heuristic passes by construction in both branches. Means AI games give the new check zero test coverage. Fine — it's insurance for a future smarter AI.
- **B3 / C1:** guards that early-returned *after* `Instantiate` left blank ghost cards parented in the layout with live prefab-serialized listeners (worse than the NRE they replaced). `ShowMulliganUI` now skips nulls and validates the component *before* wiring. Also resolved the asymmetry where `SetCardForMulligan`'s guard was reachable but `SetCard`'s and `SetMinion`'s were not.
- **C2:** dissolved on its own — `OnFaceClicked` no longer builds a Taunt list at all.
- **D:** `Minion.CanAttack` now includes `!IsDead`, so the eligibility tint and selection check can't treat a corpse as attackable.
- **E (no fix needed, but worth recording):** `List<Minion>.Contains()` reference equality confirmed safe — see Lessons Learned for the full identity chain and the important `CardData` contrast.

### Follow-up pass (same session, Claude Code) — the finding-D remainder
The fixes above closed A1–C2, but part of finding D was never applied: `BoardDisplay` got the full serialized-field treatment while `HandDisplay` did not, and neither `MinionView` nor `CardView` guarded the text fields they write into. Closed in a second pass, all five assemblies compile-verified:
- **`HandDisplay.RenderHand` brought to parity with `BoardDisplay.RenderBoard`** — guards `handPanel` *before* the child-clearing loop (the same NRE-before-the-guard problem `BoardDisplay` had), guards `cardViewPrefab`, and destroys an instantiated object whose `GetComponent<CardView>()` comes back null rather than leaving a ghost parented in the layout. Still deliberately silent on per-entry nulls, per the note in the file table above.
- **`MinionView`** — `nameText`/`statsText` were the last unguarded dereferences in the file; `button`, `minionBackground`, and `artworkImage` were already guarded.
- **`CardView`** — `nameText`/`costText`/`statsText` in `WriteCardText()`, plus `button` in both setters. `MinionView` already guarded its `button` and `CardView` did not, which was the last remaining inconsistency between the two sibling views.
- **`AfterGameAction()` now drops a dead `selectedAttacker`.** Direct consequence of the A1 fix: keeping the selection on a *rejected* attack is correct, but combined with `BoardDisplay` now skipping `IsDead` minions it meant a selected minion that died would stay selected while being invisible — unclickable, therefore undeselectable, and silently routed into every subsequent attack attempt as a permanent rejection loop. Not reachable today (nothing can kill a selected attacker mid-selection), but it becomes reachable with minion-targeting spells, exactly like A2.
- **`Combat`** — dropped the `defenderTaunts != null` half of the Taunt condition. `Board.GetTauntMinions()` returns `List.FindAll()`, which never returns null, so that half went dead the moment B1 moved the lookup inside `Combat`.

**Unverified from this session:** none of the post-review fixes have been playtested yet — including the follow-up pass above, which is compile-verified only. Compiling is not behaviour. That's the first item in Next Steps.

**Real issues identified by earlier reviews, still NOT fixed** (tracked so they aren't lost):
- `FaceView`'s idle-animation base position/scale is captured potentially before the first Canvas layout pass — could cause a permanently offset avatar with no error in some circumstances (not observed yet, just a risk).
- Both avatar `Image`s still have `Raycast Target` checked (should be unchecked per established habit); the two avatars are set up with inconsistent anchor types in the scene.
- Spells can currently only ever target the enemy face (`opponentTarget` is a single cached `Target` reused for every non-self spell) — there's no way for a human to target a minion with a damage spell. **Now promoted to a planned feature** — see Next Steps.
- No draw-game handling — `CheckWinCondition()` checks Player One first, so a simultaneous double-KO always awards the win to Player Two.
- `gameOver`/`winner` are never reset — no restart/new-game path exists yet.
- Several smaller nice-to-haves noted (0-attack minions could theoretically attack per `CanAttack`'s current logic; `GameContext.Board` and `CardData.description` are currently unused/write-only; `TurnNumber` counts half-turns not rounds).
- **`CardData` reference identity is not clean** (surfaced session 27): `BuildDeck()` adds the same ScriptableObject asset reference `copiesPerCard` times, so `Hand.Contains(card)`/`Hand.Remove(card)` and the `HashSet<CardData> mulliganSelections` all treat two copies of the same card as one object — mulligan-selecting one copy of a duplicate toggles the shared entry. Pre-existing and unrelated to any recent change, but a real behaviour worth knowing about before building anything else that identifies cards by reference.

**New loose threads noticed session 27, not investigated:**
- A repeating visual artifact where a semi-transparent row of cards renders overlapping the board row — seen in two separate screenshots on different games, so likely reproducible rather than a one-off drag artifact. Possibly the opponent's hand rendering over their board. Not diagnosed.
- The console showed **2 warnings** during one playtest; they were never expanded or investigated.

**Older, still-outstanding items:**

**Taunt — confirmed working via direct playtesting:**
- `TestCard_Shieldbearer` (1/5 Minion) had its `Has Taunt` checkbox enabled in the Inspector — no new card asset needed, just flipped the existing card's flag.
- Both boards correctly render `"Shieldbearer (Taunt)"` for any Taunt minion.
- **Rejection confirmed**: tried attacking a non-Taunt enemy minion while the opponent had a Taunt minion in play — correctly blocked, console showed `"Player Two has a Taunt minion — you must attack it first."`, and the attacker stayed selected. **Session 27**: this exact behaviour (message text *and* attacker staying selected) was briefly broken by the refactor and then deliberately restored — it should still read and behave exactly as recorded here. Confirming that is a Next Steps item.
- **Success confirmed**: attacking the Taunt minion directly afterward correctly resolved — console showed `"Shieldbearer attacked."`, and the Taunt minion's health dropped exactly as expected (1/3 → 1/2 from a 1-attack hit).
- **AI-side enforcement — now confirmed (session 27)**. Previously built using the identical `Board` helper as the human path but never observed in play. A session 27 playtest confirmed the AI attacks a Taunt minion and, once it dies, correctly re-evaluates and moves on to other targets rather than re-attacking the corpse.

**Tier 1 (fatigue/board/hand caps) — pasted in, compiles cleanly, not yet stress-tested:**
- Neither fatigue (needs a fully emptied deck) nor the board cap (needs 7 minions on one side) has come up naturally in playtesting so far. Worth a longer test session at some point to confirm both actually fire correctly when triggered.

**Session 23 — first Claude Code session, two real bugs found and fixed** (see `EffectTester.cs`/`PlayerHand.cs` rows above for full detail): a late win-check that could let a turn transition happen after the AI already won mid-turn, and a silent no-op when a card effect had no target passed in. Both fixes reviewed and confirmed sound in chat.

**Cosmetic, still low priority:**
- `BoardPanel`/`OpponentBoardPanel` text-clipping issue (minion name cut off at left edge) unaddressed.
- Board panel vertical spacing could use more separation.
- `MulliganPanel` sits in the same screen position as `HandPanel` (fine functionally, hidden after confirm).

## Lessons Learned / Gotchas (useful to remember)
**Assembly definitions (.asmdef)**
- Circular references are rejected — keep dependency direction one-way.
- An asmdef's real identity is its **Name** field (Inspector), not its filename.
- `CS0246` errors can mean either a missing asmdef reference OR a missing `using` statement.
- Cross-assembly type usage needs BOTH the `using` directive AND an explicit **Assembly Definition Reference**.

**Unity Editor / UI basics**
- Script filename must exactly match its class name. Scene changes aren't saved until `Ctrl+S`.
- For stretch-anchored panels, reposition via Rect Transform `Pos Y`/`Left`/`Right`/`Height`, not free dragging.
- A circular gizmo appearing unexpectedly in Scene view is likely just the `Global Light 2D` gizmo — editor-only.
- Editing a runtime `(Clone)` GameObject in Play mode is temporary — use Prefab Edit Mode for permanent changes.
- An empty/unassigned Inspector field for a listener-style hookup fails **silently**.
- Editing a TMP label's `Text Input` box requires clicking in, changing text, then clicking away to commit.
- `CS0102` after a manual paste-in usually means a duplicated field/header block — this is why full-file pastes are now standard.
- A script field needing a component that lives on the *same* GameObject the script is attached to: drag that same GameObject onto its own field, Unity finds the matching component.
- A scene GameObject can accidentally become a prefab instance (showing `Prefab`/`Overrides`/`Select`/`Open` at the top of its Inspector) if it's ever dragged into the Project window — easy to do by accident. For a one-off bootstrapper object never meant to be reused as a prefab (like `EffectTester`), fix via right-click → **Prefab → Unpack Completely**, then delete the now-orphaned prefab asset from the Project window.
- `UnityEngine.Random` can produce the same first result across rapid consecutive Play-mode restarts in the Editor — a known quirk of its default seeding, not a logic bug. Add an explicit `Random.InitState(System.DateTime.Now.Millisecond + System.Environment.TickCount)` before the random pick.
- A full-screen background `Image` should have `Raycast Target` unchecked proactively when created, not just fixed reactively after discovering it blocks clicks — treat as a default habit for any purely-decorative `Image`.

**Code structure / design**
- When a display/rendering component is already generic, support "two of something" via a second *instance*, not code to juggle two lists.
- A callback using the null-conditional operator (`onClicked?.Invoke(card)`) gives "free" read-only-mode support.
- `ScriptableObject`-based cards support "multiple copies in a deck" via repeated list references — no asset duplication needed. **But see the identity caveat below.**
- Use in-place Fisher-Yates for shuffling a `List<T>`.
- A boolean flag on the data itself (`CardData.targetsSelf`) is the right way to encode "who does this affect."
- A temporary/this-turn-only stat bonus needs no explicit expiry if it modifies a field some other system already resets unconditionally each cycle.
- When a UI component needs two distinct interaction modes sharing most setup, pull shared parts into a private helper and give each mode its own public entry point, rather than a mode-flag parameter on one method.
- A "confirm/complete" boolean guard flag checked at the top of interactive handlers is a simple way to gate a whole phase without a full state machine — appropriate at small scale; worth revisiting as a real state machine if more phases (e.g. combat) get added.
- When adding a toggleable alternate mode (like `manualControlMode`) to code that already has implicit assumptions baked in (like "only Player One's hand is ever clickable, so no one checks whose turn it is"), audit those implicit assumptions explicitly — new modes are a good forcing function for surfacing hidden coupling.
- **Deck design**: Hearthstone's real deckbuilding rule is many unique cards each capped at ~2 copies, not few unique cards with many copies — worth preserving that shape (add more `CardData` assets, not a higher `copiesPerCard`), and because `BuildDeck()`/`Copies Per Card` was built to scale to any pool size without code changes, growing the card pool is pure content work, not engineering work.
- **New session 27 — put the rule at the chokepoint.** If every path to an action funnels through one method, the rule governing that action belongs *inside* that method. Three call sites each doing their own Taunt check meant three places that could drift, and meant a fourth attack path (planned drag-and-drop) would silently skip the rule unless its author remembered. Moving it into `Combat.TryAttack` made bypassing it structurally impossible rather than merely discouraged.
- **New session 27 — a rule is only centralised if its *inputs* are too.** The first attempt passed the Taunt list *in* as a parameter. That still left every caller responsible for fetching the right list, passing the defender's rather than the attacker's, and not passing null — and `defenderTaunts != null` meant a null silently disabled the whole rule. Passing the `Board` and letting the method fetch its own input is what actually closed the hole. If a centralised check can be defeated by a caller passing the wrong argument, it isn't centralised yet.
- **New session 27 — moving a check changes more than the check.** Relocating the Taunt guard from `OnMinionClicked`/`OnFaceClicked` (which `return`ed early) into `ResolveAttack` (which unconditionally cleared `selectedAttacker`) silently changed rejection behaviour: a blocked attack now also dropped your selected attacker. The logic was identical; the surrounding control flow was not. When lifting a guard out of one method into another, check what the *new* location does after the guard, not just whether the guard itself is equivalent.
- **New session 27 — guard *before* you instantiate.** Several guards were placed at the top of `SetCard`/`SetMinion`, but the caller had already run `Instantiate` and parented the object. The early `return` then left a blank prefab-default card sitting in the layout with any prefab-serialized listeners still live — a silent visual artifact, strictly worse than the NRE it replaced. Validate the data (and the prefab's component) before creating the GameObject.
- **New session 27 — reference identity: `Minion` is clean, `CardData` is not.** `List<Minion>.Contains()` resolves to reference equality (`Minion` is a plain class with no `Equals`/`GetHashCode`/`operator==` override), and that's safe here because minions are never copied: `new Minion(...)` appears exactly once in the codebase (`PlayerHand`), `Board.GetTauntMinions` returns a `FindAll` (new list, same element references), `BoardMinions` is only ever `Add`ed to and `RemoveAll`ed from, and the reference survives intact all the way through `RenderBoard` → `SetMinion` → the click closure → `new Target(minion)`. **`CardData` is the opposite** — `BuildDeck` adds the same asset reference multiple times, so two "copies" of a card in hand are literally one object. Before writing anything that identifies a card by reference, remember that `Contains`/`Remove`/`HashSet` cannot tell duplicates apart.
- **New session 27 — write in one tool, review in the other.** The chat workflow wrote the refactor and introduced a behaviour regression it didn't notice; Claude Code caught it, along with a missed file and four smaller issues. Scoping the review prompt to *what actually changed* (rather than requesting another full-codebase sweep) made the findings much more targeted. Keep the "report findings before fixing anything" instruction — it makes the fix order a deliberate decision rather than an automatic one.

**Core architecture principle**
- Keep `Core` types generic and unaware of `CardData`. AI-ness isn't a flag on `Player`.

**Git / GitHub / environment**
- GitHub requires `gh auth login` or a PAT (repo scope only) for HTTPS git auth.
- `.gitignore` should exclude `Library/`, `Temp/`, `.sln`/`.slnx`, `.csproj`.
- Unity's crash-recovery prompt (`Assets/_Recovery/`) is safe to accept; delete the folder after.
- .NET SDK (install via Microsoft's apt feed, not Ubuntu's default repo) is only needed for VS Code's C# IntelliSense/debugging.
- `grep -rn "TryAttack" --include=*.cs .` from the project root is the fastest way to find every call site of a method before/after changing its signature — used repeatedly in session 27 to confirm all three sites moved to the new signature together. VS Code's `Ctrl+Shift+F` does the same with clickable results.

## Next Steps (in order)
1. **Playtest session 27's post-review fixes** — none have been run yet. Specifically confirm: (a) with a Taunt minion up, attacking a non-Taunt target is rejected **and the attacker stays selected**; (b) the rejection message names the owning player (`"Player Two has a Taunt minion — you must attack it first."`) rather than the generic form; (c) a few normal turns (attacks, hero power, card plays, end turn) produce no new console warnings.
2. **Targeting system — let spells target minions, not just face.** Build the "select a card → select a target" interaction flow. Do this *first* of the three requested features, because drag-and-drop needs the same interaction layer, and building targeting click-based then bolting drag on afterwards would mean either rewriting it or maintaining two parallel input paths. Can likely reuse the `selectedAttacker` pattern that already exists for minion attacks. **Note**: session 27's A2 fix (`AfterGameAction()` sweeping corpses on every path) is specifically what makes this safe to build now — this feature is what would otherwise have exposed that bug.
3. **More spells.** Mostly content work on the existing `CardEffect` system, but AoE / heal / buff / draw each need their own new `CardEffect` subclass. Needs a scoping pass first: decide which specific spells are wanted, then sort into "new `CardData` asset only" vs "needs a new effect class."
4. **Drag-and-drop to play cards** (replacing click-to-play). Deliberately sequenced *after* targeting, so it becomes a new input layer over a working system rather than being built simultaneously with the targeting rules. Much safer than it was pre-refactor: `Combat.TryAttack` now owns Taunt and fetches its own inputs, so a new attack path physically cannot bypass the rule. Will touch `CardView`, `MinionView`, `HandDisplay`, `BoardDisplay`, and `EffectTester` — Unity's `IBeginDragHandler`/`IDragHandler`/`IEndDragHandler`, drop zones, a drag ghost, and canvas raycasting.
5. Investigate the overlapping semi-transparent card row artifact (seen twice, likely reproducible).
6. Expand and investigate the 2 console warnings seen during session 27 playtesting.
7. Generate art for the remaining 14 cards (only Goblin has real art so far).
8. Consider generating more board background variety and/or more music tracks (currently 3 backgrounds, 1 music track).
9. Longer playtest to actually trigger and confirm fatigue and the 7-minion board cap.
10. Quick explicit check: same minion attacking twice in one turn should be rejected (logic looks correct per code review, not separately re-verified in play).
11. Re-test manual control mode's turn-gating fix (written, never re-tested).
12. Address the still-outstanding `FaceView` issues: base position/scale possibly captured pre-layout; both avatar `Image`s have `Raycast Target` checked; inconsistent anchor setup between the two avatars.
13. Consider smarter AI attack logic (trading/risk awareness). Note this is exactly when the centralised Taunt check stops being a no-op on the AI path.
14. Consider adding a draw-game outcome (currently a simultaneous double-KO always awards Player Two the win) and a restart/new-game path (`gameOver`/`winner` are never reset currently).
15. **Remaining Tier 2 content work**: Deathrattle, Charge/Rush, Divine Shield, Windfury, Silence, full Hero classes + real Hero Powers (current one is a deliberate minimal placeholder), and Weapons — each needs its own scoping/design pass before building.
16. Record `BoardPanel`'s corrected `Pos Y` value and `HandPanel`'s current Rect Transform values for documentation completeness.
17. Delete `EffectTester`/rename to something like `GameBootstrapper` once real play/board interaction replaces the manual test setup.
18. Consider simple visual effects/animation feedback (flashes, particles for spells/attacks) as a further visual layer.
19. Fix the `TesCard_Watchman` asset-name typo, if desired.

## Git Habits Being Followed
- Simple commit template: `git add .` / `git commit -m "short one-line summary"` / `git push`
- Push regularly to `github.com/craig-middleton/hearthClone`
