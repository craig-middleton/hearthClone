# HearthstoneClone — Project Status

> **Current state only.** For *why* — session narratives, past bugs, Editor gotchas, asset pipelines, tooling setup — see `PROJECT_HISTORY.md`.

_Last updated: 2026-08-27 (AI plan step 4 of 5 — lethal-detection override built and wired into `AIController.TakeTurn()`, ahead of and separate from the favorable/unfavorable-trade evaluation: if `board.GetTauntMinions(opponent)` is empty and the sum of `CurrentAttack` across all `CanAttack` AI minions is >= `opponent.Health`, every attacker swings face instead of being routed through the trade-evaluation branch, so a lethal attacker can no longer be diverted into a "good" trade. Compile-verified (`dotnet build AI.csproj`); a Scenario 4 playtest attempt this session (lethal unblocked damage, no Taunt) did not complete cleanly — repeated manual-control staging errors ran out session time — so step 4 is **not** playtest-confirmed yet (see Verification Status). Also recording step 3 (Taunt pre-filter regression check under the new trade logic), confirmed via playtest this session with no new code: AI correctly attacked a Taunt minion (Shieldbearer 1/5, killed) over a tempting favorable trade against an untouched Murloc, with Charging Rhino surviving at 5/4. See Code Written So Far, Verification Status, and Next Steps 12. Previously, session 33 — `EffectTester` decomposition (Next Steps 16) DONE: all 4 components extracted, compile-verified, and playtest-confirmed (`MulliganController`, `CombatInputController`, `CardDragResolver`, `GameManager`). `EffectTester` itself is down to 249 lines from 643 pre-decomposition — bootstrapper, composition root, and view refresher only. Constraint 11 (the old "delete/rename to `GameBootstrapper`" note) marked resolved: the decomposition superseded a straight rename, since a new name alone wouldn't have addressed the seven-responsibility bloat the constraint was really about. Pattern held across all four extractions: explicit constructor references for shared model state, `Func<bool>` predicates for cross-cutting gates (`gameOver`/`mulliganComplete`/`manualControlMode`), `Action` callbacks (some as lambdas, to break a real construction-order cycle between `GameManager` and `CombatInputController` — see the `EffectTester.cs` row) in place of sibling method calls. One recurring cost noted once rather than per-extraction: plain C# controllers can't pass `this` as `Debug.Log` context (Constraint 19). `CombatInputController` deliberately left the already-open own-side-rejection gap unfixed, as decided before the split began; `CardDragResolver`'s highest-risk playtest item (an exactly-lethal spell hit, the destroyed-`Transform` bug class from Constraint 16) and `GameManager`'s highest-risk item (the session-31 six-hardcoded-reference bug class, re-checked for Player Two's Hero Power) both re-confirmed clean through their extracted paths. See Next Steps 16.)_

## How to use these files
- **`PROJECT_STATUS.md` (this file)** — give it to a new Claude chat at the start. Current truth, stands alone: enough to start the next feature.
- **Upload the file rather than pasting it.** Uploading avoids truncation entirely; pasting is what silently drops the middle of a long file, and the chat does not notice it happened.
- **`PROJECT_HISTORY.md`** — read for *why*: which bug a guard prevents, what a refactor abandoned, how the pipelines work.
- **If a session claims something isn't documented, check HISTORY before believing it.** A chat given only STATUS lacks the history, and pastes can truncate silently.
- **New work goes to HISTORY.** In STATUS only current-state sections are *updated in place*. STATUS must not grow.

## Working Preferences
- A 2-paragraph explanation after each new/updated code block: what it does and why.
- **Chat sessions only: full rewritten class/file, not a diff** — Craig pastes whole files and partial merges cause errors. **Not for Claude Code sessions**, which edit on disk: there a whole-file rewrite is overhead and an unreadable diff. Make targeted edits.
- **Avoid singletons** — explicit constructor/field/parameter-passed references over global static instances. Targets *global static state*, not parameters: passing `Board` into a method is the preferred style.

## Project Goal
Hearthstone-style card game in Unity, single-player vs AI, C# with data-driven cards/effects (ScriptableObjects). Ubuntu, Unity + VS Code, Git/GitHub with LFS for art/audio. Repo: `github.com/craig-middleton/hearthClone`.

**Unity 6.5 (60000.5.2f1).** TextMeshPro for all UI text, never legacy `UnityEngine.UI.Text`.

## Architecture
- Folders under `Assets/_Project/`: Scripts, ScriptableObjects, Prefabs, Art, Scenes. Cards are data-driven — they reference reusable `CardEffect` assets.
- **Assembly Definitions** enforce one-way flow: `Core` (no deps) ← `Effects` (Core) ← `Cards` (Core, Effects) ← `AI` (Core, Cards, Effects) ← `UI` (Core, Cards, Effects, **AI**, TMP). `Effects` must NOT reference `Cards`; `UI` references `AI` because `EffectTester` constructs `AIController`.
- **Keep `Core` generic and unaware of `CardData`.** AI-ness isn't a flag on `Player`.

### Design principles in force
Named only; reasoning and worked examples in HISTORY under Lessons Learned.
- **Rules live at the chokepoint** · **a rule is only centralised if its *inputs* are too** (`TryAttack` derives the defender rather than accepting it) · **guard *before* you instantiate** · **hiding something from the view doesn't remove it from state** · **reference identity: `Minion` is safe, `CardData` is not** · **"two of something" is a second *instance***, not code juggling two lists.

## ⚠️ Live Constraints
Read before changing the named code. Kept above the table deliberately — a truncated paste loses the *end* of a long table, and these are the parts that must not go missing.

1. **`Board.GetTauntMinions(Player)` MUST NEVER RETURN NULL.** `Combat.TryAttack` calls `.Count` on it directly, so a null-returning guard here reintroduces an NRE there. `List.FindAll` already returns an empty list — the correct "no Taunts" answer.
2. **`Board.GetTauntMinions` has two call sites**: `Combat.TryAttack` (the rule) and `AIController` (its targeting heuristic). The *rule* lives in one place, the *lookup* in two — **changing it means checking both.**
3. **`Board.GetOwnerOf(Minion)` returns null** when the minion is on neither board. Callers must handle null.
4. **`Combat.TryAttack` guard order**: null attacker/target → null board → malformed `Target` → derive defender → attacker `IsDead` → target minion `IsDead` → defender null → own-side → Taunt → `CanAttack`. Taunt sits *above* `CanAttack` for message precedence; dead-attacker above both, since `CanAttack` includes `!IsDead` and would otherwise blame "summoning sickness" for a corpse.
5. **Accepted gap in `Combat.TryAttack`**: an attacker on neither board passes the own-side check (`GetOwnerOf(attacker)` is null, `defender` isn't). `attacker.IsDead` covers the realistic case; the rest needs a caller fabricating an off-board `Minion`. Deliberately unguarded.
6. **`EffectTester.AfterGameAction()` order**: `RemoveDeadMinions()` → drop a dead `selectedAttacker` → `RefreshAll()` → `CheckWinCondition()`. The drop must stay *before* the refresh. Called by *every* state-changing path.
7. **`EffectTester.ResolveAttack` clears `selectedAttacker` only on success** — a rejected attack deliberately keeps the selection for retargeting.
8. **`HandDisplay.RenderHand` and `BoardDisplay.RenderBoard` are structurally identical by design** — same guards in the same order. A change to one must be mirrored in the other. **Deliberate exception, current shape as of session 32**: `HandDisplay.RenderHand` takes the three drag callbacks (`onCardDragEnded`, `onCardDragBegan`, `canDrag`) plus `List<CardData>`; `BoardDisplay.RenderBoard` takes `onMinionClicked`/`selectedAttacker`/`showAttackEligibility` plus `List<Minion>` — no drag callbacks, since minions aren't dragged, only cards are. The old `int` hand-index parameter (session 31, for click-based pending-selection identity) is gone along with the click flow it served — see old Constraint 17. Still accepted, still not a missed mirror: the two signatures diverge because hand cards and board minions are dragged/clicked differently, not because one was updated and the other forgotten.
9. **Which view guards log**: `CardView` (`nameText`/`costText`/`statsText`/`button`), `MinionView` (`nameText`/`statsText`/`button`) and `FaceView` (`healthText`/`button`) log a named warning with `this` as context. `artworkImage`, `cardBackground`, `minionBackground` and `avatarImage` are guarded but **deliberately silent** — unassigned is a valid configuration for decorative fields.
10. **`BoardDisplay` skips `IsDead` minions** as defence-in-depth against a future damage path forgetting to sweep.
11. **RESOLVED, session 33 — not literally followed, superseded by a better plan.** Originally read: "`EffectTester` is temporary — delete or rename to `GameBootstrapper` once a real gameplay loop exists." A real gameplay loop has existed since session 32; the rename was never done, because a straight rename wouldn't have addressed *why* this constraint existed — `EffectTester` had grown to 643 lines owning seven unrelated responsibilities (deck/hand construction, mulligan UI, combat input, drag-to-play resolution, turn/win flow, Hero Power, audio/background setup), and a new name wouldn't have changed that shape. Session 33 instead did the decomposition the rename was gesturing at: four components extracted (`MulliganController`, `CombatInputController`, `CardDragResolver`, `GameManager` — see Next Steps 16, fully done and playtest-confirmed), leaving `EffectTester` at 249 lines as a genuine bootstrapper/composition-root/view-refresher — the role a `GameBootstrapper` rename would have only implied, not delivered. No further rename is planned; `EffectTester` earned its current name back by actually being one now.
12. **Older `CardData` assets physically omit later-added fields.**
    - **State**: `hasTaunt` is absent from **14 of the 16** assets (present only on `TestCard_Shieldbearer` as `1` and `TestCard_Goblin` as `0`). `artwork` and `description` are present on all 16. (`targetsSelf` used to be absent from 4 assets here as an example of this hazard — it no longer exists, having been renamed to `targetRequirement` in session 31 and hand-written into every asset that needed it; see Card Pool.)
    - **Mechanism**: adding a field to `CardData.cs` does **not** rewrite existing assets. An asset's YAML is written only when Unity saves *that asset* — an Inspector edit, a scripted write, or a forced re-serialize — and until then it keeps whatever field set it had when last written. Missing fields fall back to the C# initializer at load. Git shows it cleanly: `targetsSelf` landed in `48e9d98`, and the ten-card batch created two commits later (`8963670`) has it; `hasTaunt` landed in `b2c217f`, five commits *after* that batch, so every asset lacks it except the two re-saved since. The clincher is `de18de0`, where assigning Goblin's artwork wrote **both** `artwork:` and `hasTaunt: 0` into the file in one diff — nobody touched Goblin's Taunt checkbox; the whole asset was simply re-serialized.
    - ✅ **New assets are fine.** A card asset created *after* a field exists gets that field written at creation — which is exactly why the ten-card batch has `targetsSelf`. The hazard is confined to assets that already exist when the field is added.
    - **Hazards**: grepping the assets for a field name misses most cards (`grep hasTaunt` returns 2 of 16); and changing a field's initializer in `CardData.cs` silently flips every asset that omits the field, while leaving the explicitly-written ones alone.
    - **Remedy**: force re-serialization — `AssetDatabase.ForceReserializeAssets` on the `CardData` assets, or touching each one in the Inspector — which writes the omitted fields physically into every file. ⚠️ **Do this BEFORE changing any field's initializer, never after.** Afterwards the silent flip has already happened, and re-serializing only pins the wrong values in permanently.
13. **`.csproj` files are gitignored and Unity-generated.** Fine for editing existing scripts, but adding a new script and building without letting Unity regenerate would build clean while silently omitting it — open Unity after adding files.
14. **A child positioned with point anchors (`AnchorMin == AnchorMax`) and a fixed `SizeDelta` does NOT track its parent's actual resolved size** — its size is absolute, set once, independent of whatever `HorizontalLayoutGroup`/`VerticalLayoutGroup` later assigns the parent. `MinionView`'s `ArtworkImage` was point-anchored with `SizeDelta` hand-matched to the *design* size (160x220); under `BoardPanel`'s `HorizontalLayoutGroup` (`Control Child Size` on, no min width set on the `LayoutElement`), a board with enough minions to exceed the panel's width shrinks every `MinionView` root below 160x220 — but `ArtworkImage`, unaffected by the layout group (it's a grandchild, not a direct child), stayed at the fixed 160x220 and overflowed past the shrunk card. Invisible on minions with no artwork (`Image.enabled = false`), so only artwork-bearing minions (Goblin) appeared to render "bigger" than their neighbours, though the root `RectTransform` sizes were identical the whole time. **Fix**: `ArtworkImage` now uses stretch anchors (`AnchorMin {0,0}` / `AnchorMax {1,1}`, `SizeDelta {0,0}`) so it always exactly fills whatever size the root actually resolves to. **Any child meant to fill `MinionView` or `CardView`'s root must use stretch anchors, not point anchors with a size that merely matches today's design dimensions** — the design dimensions and the runtime-resolved size are only the same number by coincidence, not by construction.
15. **`SpellAnimationSequencer` reads a `CardView`'s position as a `Vector3` snapshot, never a live `Transform`.** `HandDisplay.RenderHand` destroys and rebuilds every `CardView` synchronously inside `AfterGameAction()`, in the same frame the card was played — the exact `CardView` a spell's travel effect launches from will not exist a frame later. The target side is still tracked live (`FaceView` persists across refresh, confirmed; `MinionView` does not — see Constraint 16 for what that costs and how it's handled). This is also why lift/zoom and discard-off (Next Steps) are blocked until `RefreshHandDisplay`'s destroy timing is addressed — see that Next Steps entry for the two options on the table.
16. **A lethal spell hit on a minion can destroy the target's `Transform` mid-animation — `SpellAnimationSequencer` must tolerate that, not assume the target survives.** Originally found via `EffectTester.ResolveSpell` (session 31, since removed — see Constraint 17); the same fix and same risk apply unchanged through `ResolveCardDrag` (session 32), which starts the travel coroutine (holding a live target `Transform`) *before* `AfterGameAction()` runs, identically; `AfterGameAction` calls `RemoveDeadMinions()` then `RefreshAll()` synchronously in the same frame, which destroys the dead minion's `MinionView` before the coroutine's next `yield` resumes — an exactly-lethal hit throws `MissingReferenceException` on the following frame's `targetView.position` read. **Fix**: `TravelAndReactRoutine` re-checks `targetView != null` every frame (Unity's overloaded operator correctly flags a destroyed-but-not-nulled `Object`) and falls back to the last position sampled before it died; the end-of-travel reaction call is skipped the same way if the target is gone. **Chosen over unconditionally snapshotting the target's position at cast time** (the same treatment already given the source side) because the target survives intact in the overwhelming majority of casts — only a lethal kill destroys it — and is deliberately tracked live so `FaceView`'s idle sway shows through during travel; an unconditional snapshot would silently kill that sway for every cast, not just lethal ones, to guard a one-frame-rare edge case. **Confirmed minion-only**: `FaceView` is never destroyed by a refresh (`RefreshFaceDisplay` only calls `SetPlayer()` on the existing instance), so a lethal face hit cannot hit this bug — playtest-confirmed, not assumed.
17. ~~A hand card's pending-selection identity is tracked by its index in `Hand`...~~ **Removed session 32.** `pendingSpellCard`/`pendingSpellHandIndex`/`pendingSpellSourcePosition`/`pendingSpellCaster` and the hand-index identity scheme they depended on no longer exist — click-to-play (and the click-then-click targeting flow it enabled) was fully replaced by drag-and-drop, which resolves in a single gesture and has no cross-click state to disambiguate. See Constraint 18 and Next Steps 4.
18. **`boardPanel`/`opponentBoardPanel` need a raycastable `Graphic` covering their full bounds, or `EventSystem.RaycastAll` returns zero hits over empty board space.** uGUI's graphic raycasting only hits GameObjects that carry a `Graphic` (`Image`, etc.) with `raycastTarget` on — a bare `RectTransform` panel used only as a layout container isn't hit-testable itself, regardless of what's visually rendered under it (in this case a separate, decorative `boardBackgroundImage`, not a child of either board panel). Drag-and-drop's `ResolveCardDrag` (Code Written So Far, `EffectTester.cs`) needs the board panels to register a hit for None/Self-target spells and minion summons dropped on open board space, not just on an existing `MinionView`/`FaceView` — those already had their own raycastable `Graphic` and were never affected. **Fix**: `BoardDisplay.Awake()` → `EnsureRaycastCatcher()` adds a fully transparent `Image` (`raycastTarget = true`, alpha 0) directly on `boardPanel`'s own GameObject, not a child — `RenderBoard()` destroys all *children* of `boardPanel` every refresh, so a child-based catcher would be destroyed and need re-adding every render; the panel's own GameObject survives refreshes untouched. Runs once per `BoardDisplay` instance, so both boards get it automatically with no scene/Inspector editing. **Any future feature adding another droppable/clickable zone must confirm that zone has a raycastable `Graphic` covering its full area, not just assume the visible art makes it hittable.** **Diagnostic note**: the bug first surfaced as an apparent Player-Two-only gating failure (dragging Player Two's own card produced total console silence), which led the investigation down a wiring/gating path before a `[DragDiag]` trace showed the callback firing correctly and rejecting on zone-recognition — the real bug was universal to both players and specific to empty board space, not to Player Two at all. **A reported symptom narrower than the actual bug is common**; testing the wider case (here: "does Player One's drop on empty board space also fail?") is what surfaced the true scope.
19. **`EffectTester` split (session 33+): extracted controllers are plain C# classes, not `MonoBehaviour`s, and that has one recurring cost — `Debug.LogWarning`/`Debug.LogError` calls inside them can no longer pass `this` as the context object** (the click-to-highlight-the-source-object console feature), because only a `UnityEngine.Object` can be a log context and a plain C# class isn't one. `MulliganController` is the first case (session 33). **This will recur for every future extraction** (`CombatInputController`, `CardDragResolver`, `GameManager`) — noting it once here as a pattern rather than re-flagging it per component. Logs still fire with the same message text; only the click-to-select-object convenience is lost.

## Code Written So Far
Constraint notes live above; these rows say what each file does.

| File | Location | Purpose |
|---|---|---|
| `CardEffect.cs` | `Scripts/Effects/` | Abstract SO base; `Execute(GameContext, Target)`. |
| `DealDamageEffect.cs` | `Scripts/Effects/` | Deals `damageAmount`; logs remaining health. |
| `GainManaEffect.cs` | `Scripts/Effects/` | Calls `target.GainMana(manaAmount)`. |
| `CardData.cs` | `Scripts/Cards/` | SO: `cardName`, `description` (never read), `artwork`, `manaCost`, `cardType`, `attack`, `health`, `hasTaunt`, `onPlayEffect`, `targetRequirement` (session 31, replaced the old `targetsSelf: bool` — `TargetRequirement { None, Self, Any }`, see Card Pool). Session 32: `TargetRequirement`'s doc comment updated to describe drag-drop resolution (`EffectTester.ResolveCardDrag`) instead of the removed click handlers — same enum, same meaning, only the consumer changed. |
| `Minion.cs` | `Scripts/Core/` | Board minion: `HasSummoningSickness` (true from construction), `HasAttackedThisTurn`, `HasTaunt`, `Artwork`, `ResetForNewTurn()`. `IsDead` = `CurrentHealth <= 0`. `CanAttack` = `!IsDead && !HasSummoningSickness && !HasAttackedThisTurn` — read by `MinionView`'s tint, `EffectTester`'s selection, and `AIController`'s attack loop over a pre-attack snapshot, where `!IsDead` stops it swinging with a corpse. No `CurrentAttack > 0` term. |
| `Player.cs` | `Scripts/Core/` | `PlayerName`, `Health` (30), `CurrentMana`/`MaxMana`, `BoardMinions`, `FatigueDamage`, `HasUsedHeroPowerThisTurn`, `TakeDamage(int)`. No `IsAI` flag. |
| `Board.cs` | `Scripts/Core/` | Both `Player`s; `GetOpponent()`, `RemoveDeadMinions()`, `GetTauntMinions()`, `GetOwnerOf()` — the last two XML-documented. Constraints 1–3. |
| `GameContext.cs` | `Scripts/Core/` | Holds a `Board` reference. Assigned in the ctor, never read — write-only. |
| `Target.cs` | `Scripts/Core/` | A `Player` **or** a `Minion`. `TakeDamage()`, `GetCurrentHealth()`, `GainMana(int)` (uncapped). Built with a null argument it is non-null with *both* fields null — `TryAttack` rejects that. |
| `Combat.cs` | `Scripts/Core/` | Static, stateless. `TryAttack(Minion attacker, Target target, Board board, out string failReason)`. **Sole owner of the Taunt rule**; derives the defender internally (`target.TargetPlayer` for faces, `board.GetOwnerOf(...)` for minions) so no caller can select the wrong side. On success applies damage, and strikes back if the target is a minion (simultaneous). Constraints 4–5. |
| `TurnManager.cs` | `Scripts/Core/` | Turn order + mana. Private `StartTurnFor(player)` refills mana, resets minions, clears `HasUsedHeroPowerThisTurn`; called from `StartGame()` and `EndTurn()`, so a minion stays sick through the opponent's whole turn. **`MaxMana` capped at 10.** `TurnNumber` = half-turns. |
| `PlayerHand.cs` | `Scripts/Cards/` | Wraps a `Player` with `Deck`/`Hand`. `Shuffle()` (Fisher-Yates), `DrawOpeningHand()`, `AddCardToHand()`, `MulliganCards(List<CardData>)` (batched — see Known Issues/mulligan-redraw fix below), `DrawCard()` (escalating fatigue when empty; burns a draw at hand size 10). `PlayCard()` blocks Minion plays at board size 7, before mana/hand are touched. **Only `new Minion(...)` site.** |
| `EffectTester.cs` | `Scripts/UI/` | **Down to 249 lines (from 643 pre-decomposition).** Bootstrapper + composition root + view refresher, and nothing else — exactly the shape the split was aiming for. `Start()`'s bootstrap sequence (decks/hands, opening hands + Coin, `mulliganController`/`combatInputController`/`cardDragResolver`/`gameManager` construction and wiring, button listeners, `SetRandomBoardBackground`/`SetRandomMusic`/`BuildDeck`), `OnMulliganComplete` (the one callback that couldn't move — needs `turnManager` for its log line), and the three `RefreshX` view-refresh methods (`RefreshAll`/`RefreshHandDisplay`/`RefreshBoardDisplay`/`RefreshFaceDisplay`, staying here permanently per the plan — they're the one piece that legitimately needs to read every other controller's public surface). All gating/state reads now go through the four controllers' public surfaces (`mulliganController.MulliganComplete`, `combatInputController.SelectedAttacker`, `cardDragResolver.DragInProgress`, `gameManager.GameOver`) rather than local fields. `using UnityEngine.EventSystems;` and `using HearthstoneClone.Effects;` dropped session 33 — both were only needed by code that has since moved out. **Construction-order note (session 33, `GameManager` step)**: `combatInputController`/`cardDragResolver` are constructed *before* `gameManager` (they need it as their `onAfterAction` callback), but `gameManager` needs a reference to the already-constructed `combatInputController` (for `ClearSelectionIfDead`/`ClearSelection`) — resolved with `() => gameManager.AfterGameAction()` lambdas rather than direct method-group references: the lambda captures the `gameManager` field by reference, not by value, so it resolves correctly once assigned a few lines later, since none of these delegates are ever invoked during `Start()` itself. Fourth and final step of the `EffectTester` decomposition (Next Steps 16) — see that entry. |
| `GameManager.cs` | `Scripts/UI/` | **New, session 33 — final piece of the `EffectTester` decomposition.** Plain C# class, constructed once in `EffectTester.Start()` with `board`/`turnManager`/`playerOne`/`playerTwo`/`playerOneHand`/`playerTwoHand`/`aiController`/`combatInputController`/`gameOverText` by reference, `Func<bool> isMulliganComplete`/`isManualControlMode` predicates, and `Action onRefreshAll` → `EffectTester.RefreshAll`. Owns `GameOver` (public getter, private setter — replaces the old `gameOver` field) and `winner`, plus `OnHeroPowerClicked`/`AfterGameAction`/`OnEndTurnClicked`/`DrawForCurrentPlayer`/`CheckWinCondition` and the `HeroPowerCost` const — all moved verbatim, logic unchanged, only field reads becoming constructor-injected references/predicates. `AfterGameAction` calls `combatInputController.ClearSelectionIfDead()` directly (no longer an `EffectTester` pass-through) — Constraint 6's ordering (`RemoveDeadMinions()` → drop dead selection → refresh → check win) is preserved exactly, just fully contained in one class now instead of split across `EffectTester` and a callback into it. `heroPowerButton`/`endTurnButton` in `EffectTester.Start()` now wire `gameManager.OnHeroPowerClicked`/`gameManager.OnEndTurnClicked` directly as the button listeners, instead of an `EffectTester`-local method. |
| `MulliganController.cs` | `Scripts/UI/` | **New, session 33.** Plain C# class (not a `MonoBehaviour`), constructed once in `EffectTester.Start()` with `PlayerHand playerOneHand, Transform mulliganPanel, GameObject cardViewPrefab, Button confirmMulliganButton, Action onMulliganComplete`. Owns the mulligan selection/confirm flow and the `MulliganComplete` flag (public getter, private setter) — wires `confirmMulliganButton.onClick` itself in its constructor. `ShowMulliganUI()` has an early-exit skip path (unwired `mulliganPanel`/`cardViewPrefab`) that also sets `MulliganComplete = true` and invokes `onMulliganComplete` — as of this session that path now fires the turn-announcement log too, same as the real confirm path, which it didn't before (harmless, console-only). Constraint 19 (log context loss). |
| `CombatInputController.cs` | `Scripts/UI/` | **New, session 33.** Plain C# class, constructed once in `EffectTester.Start()` with `Board board, TurnManager turnManager, Player playerOne, Player playerTwo`, three `Func<bool>` predicates (`isGameOver`/`isMulliganComplete`/`isManualControlMode`), and two `Action` callbacks (`onSelectionChanged` → `RefreshBoardDisplay`, `onAfterAction` → `GameManager.AfterGameAction()`). Owns `selectedAttacker` (exposed read-only via `SelectedAttacker`), `OnMinionClicked`/`OnFaceClicked`/`ResolveAttack` — logic unchanged from the pre-extraction version, only the gating reads changed from direct field access to `Func<bool>` calls. `ClearSelectionIfDead()` and `ClearSelection()` are the two ways `GameManager` reaches back in (dead-check on every game action vs. unconditional reset on turn end — see the `GameManager.cs` row). **Deliberate, explicitly accepted gap carried through unchanged**: the own-side attack rejection path in `Combat.TryAttack` remains unreachable from the UI — own-minion clicks route to selection, own-face clicks are blocked by the owner check in `OnFaceClicked` — same as before this class existed. This was a known-open item (Next Steps 1c) *before* the extraction and was deliberately left open *through* it, not fixed as part of the split: the gap is about UI reachability, and moving the code to a new class doesn't change what's reachable either way. Playtest-confirmed (session 33) — see Verification Status. |
| `CardDragResolver.cs` | `Scripts/UI/` | **New, session 33.** Plain C# class, constructed once in `EffectTester.Start()` with `playerOne`/`playerTwo`/`board`/`context`/`playerOneHand`/`playerTwoHand`/`turnManager`/`boardDisplay`/`opponentBoardDisplay`/`faceView`/`opponentFaceView`/`spellAnimationSequencer` by reference, the same three `Func<bool>` predicates as `CombatInputController`, and one `Action onAfterAction` → `GameManager.AfterGameAction()`. Owns `dragInProgress` (exposed read-only via `DragInProgress`, read by `EffectTester.RefreshHandDisplay`'s mid-drag guard), `OnCardDragBegan`/`OnCardDragEnd`/`OnOpponentCardDragEnd`/`CanPlayerOneDrag`/`CanPlayerTwoDrag` (all public — `RefreshHandDisplay` passes them straight through to `HandDisplay.RenderHand`), and the private `ResolveCardDrag`/`IsUnderPanel`/`ResolveViewTransform`/`TriggerSpellAnimation` chain, moved verbatim from the pre-extraction `EffectTester` — logic unchanged, only the field accesses became constructor-injected references/predicates. **Not a "finished" component even though extracted** — Next Steps 17's card lift/zoom and discard-off work will touch `ResolveCardDrag`/`TriggerSpellAnimation` again once the Constraint 15 decision (how `HandDisplay.RenderHand`'s synchronous `CardView` rebuild is handled) is made; this extraction only relocated the code, it didn't resolve that open design question. Constraints 15, 16, 18 (all still describe this code's actual mechanism, now living here instead of `EffectTester`). |
| `CardView.cs` | `Scripts/UI/` | One card; `SetCard()` (play) and `SetCardForMulligan()` (toggle-select) share a private `WriteCardText()`; both guard `cardData`. `artworkImage.enabled = false` hides the slot when a card has no art. Constraint 9. **Session 32**: `SetCard` dropped its click callback and `isPendingSpell`/`pendingSpellColor` tinting entirely (dead once click-to-play was removed) — `button` is now unwired for play, only `SetCardForMulligan` still uses it. `CardView` now implements `IBeginDragHandler`/`IDragHandler`/`IEndDragHandler` directly (not a separate drag-handler component — kept consistent with `MinionView`/`FaceView`, which also own both visuals and input wiring). `SetCard(CardData, Action<CardData,CardView,PointerEventData> dragEndedCallback, Action<CardData,CardView> dragBeganCallback, Func<bool> canDragPredicate)`: `OnBeginDrag` checks `canDrag` first (declines silently — no ghost — when ineligible, e.g. wrong turn) then clones its own GameObject as a semi-transparent, non-raycastable ghost (`CanvasGroup.blocksRaycasts = false`) parented under the root `Canvas` so it survives independently of `handPanel`; `OnDrag` tracks the pointer via `RectTransformUtility.ScreenPointToWorldPointInRectangle`; `OnEndDrag` destroys the ghost and invokes `onDragEnded`, guarded by a `dragActive` flag so a `canDrag`-declined drag (which Unity's EventSystem still calls `OnEndDrag` for) doesn't fire the callback. |
| `MinionView.cs` | `Scripts/UI/` | One board minion. Clickable; `minionBackground` tints selected/eligible; `nameText` appends `" (Taunt)"`. `SetMinion()` takes optional `clickCallback`, `isSelected`, `showAttackEligibility`; guards `minionData`. Constraint 9. No `costText` field — board minions have never shown mana cost (confirmed session 30; not a regression). Prefab: root `RectTransform` now 160x220, matching `CardView`'s root exactly; `ArtworkImage` stretch-anchored to fill it (Constraint 14). `PlayDamageReaction()` (session 31, mirrors `FaceView`'s) flashes `minionBackground.color` to `damageFlashColor` and back over `reactionDuration`; unlike `FaceView` it captures whatever color is currently showing (selected/cannot-attack/normal) as the flash baseline, since there's no `Update()`-driven recompute to just let overwrite it. **Session 32**: exposes `public Minion Minion => minion;` so `EffectTester.ResolveCardDrag` can build a `Target` from a drag-drop raycast hit. |
| `HandDisplay.cs` | `Scripts/UI/` | `RenderHand(List<CardData>, Action<CardData,CardView,PointerEventData> onCardDragEnded = null, Action<CardData,CardView> onCardDragBegan = null, Func<bool> canDrag = null)` — generic, two instances. Guards `handPanel` *before* the child-clearing loop, returns early on a null list, guards `cardViewPrefab`, skips null entries, destroys an object whose `GetComponent<CardView>()` is null. **No** per-entry null logging — it would spam every refresh. Constraint 8. **Session 32**: signature rewritten for drag — dropped the click callback and hand-index (`pendingSpellIndex`) entirely, since the click-then-click flow they served no longer exists (Constraint 17); added the three drag callbacks, threaded straight through to each `CardView.SetCard()`. |
| `BoardDisplay.cs` | `Scripts/UI/` | `RenderBoard()` — optional `onMinionClicked`, `selectedAttacker`, `showAttackEligibility`, passed to `MinionView.SetMinion()`. Generic, two instances, over `boardPanel`/`minionViewPrefab`. Constraints 8, 10. **Session 31**: also builds a `Dictionary<Minion, Transform>` each render (cleared and repopulated, since views are destroyed/rebuilt every call — a resolved `Transform` is only valid until the next refresh) and exposes it via `GetViewTransform(Minion)`, so `EffectTester.ResolveViewTransform` can resolve a spell target that's a minion, not just a face. **Session 32**: `Awake()` → `EnsureRaycastCatcher()` adds an invisible raycast-catching `Image` directly on `boardPanel` — see Constraint 18. |
| `FaceView.cs` | `Scripts/UI/` | Health + mana display, doubling as the face attack target. `SetPlayer(Player, Action<Player>)` writes `"{PlayerName}: {Health} HP\nMana: {CurrentMana}/{MaxMana}"` and wires a `Button`; guards `playerData`. Idle animation in `Update()`: sine scale pulse + sway, base captured on first `SetPlayer()`, four tunable fields. Constraint 9. `PlayDamageReaction()` flashes `avatarImage.color` to `damageFlashColor` and back over `reactionDuration`, called by `SpellAnimationSequencer` on impact — deliberately color-only, not position/scale, so it doesn't fight `Update()`'s per-frame sway/breathe. Confirmed (session 31) never destroyed by any refresh — see Constraint 16. **Session 32**: exposes `public Player Player => player;` so `EffectTester.ResolveCardDrag` can build a `Target` from a drag-drop raycast hit. |
| `SpellAnimationSequencer.cs` | `Scripts/UI/` | Spell-cast animation vertical slice (travel + impact reaction; lift/zoom and discard-off not built — Constraint 15, Next Steps). `PlayTravelAndReaction(Vector3 sourceWorldPosition, Transform targetView, bool isDamage)` spawns a plain runtime `Image` (no prefab/particle asset), lerps it to `targetView` over `travelDuration`/`travelCurve` (re-sampling `targetView.position` every frame since `FaceView` sways concurrently, falling back to the last known position if `targetView` has been destroyed mid-travel — Constraint 16), destroys it, then calls `PlayDamageReaction()` on whichever of `FaceView`/`MinionView` is present when `isDamage` (skipped if the target was destroyed). No heal/buff branch yet — no `CardEffect` subtype to key off. Called from `EffectTester.TriggerSpellAnimation`/`ResolveViewTransform`. **Playtest-confirmed against minions as well as faces (session 31), including the exactly-lethal case.** |
| `AIController.cs` | `Scripts/AI/` | `PerformMulligan()` (collects cards at/above a mana threshold, then calls `PlayerHand.MulliganCards()` once — batched, same fix as the player path) and `TakeTurn()` (card-play loop, then attacks). Sweeps `RemoveDeadMinions()` after the card-play loop and after every swing. **AI plan step 4** (this session): immediately after that sweep, and before the attack loop starts, computes `lethalAvailable` once for the turn — true only if `board.GetTauntMinions(opponent)` is empty *and* the sum of `CurrentAttack` across every AI minion with `CanAttack` is >= `opponent.Health`. Attack loop breaks early if the opponent is dead, skips null and non-`CanAttack` minions, targets the first opponent Taunt if any exist (this check re-runs per-minion and still wins even when `lethalAvailable` is true, since a Taunt present at loop-start would have already zeroed `lethalAvailable` — Taunt can never coexist with lethal by construction); otherwise, if `lethalAvailable`, attacks face directly, bypassing trade evaluation entirely so a lethal attacker can't be diverted into a trade; otherwise falls through to the pre-existing trade logic (AI plan step 1) — scans the opponent's board via `AICombatEvaluator.IsFavorableTrade` and attacks the first favorable target found, falling through to face only if none exists. **AI plan step 2**: immediately after the `IsFavorableTrade` scan, if the candidate it picked also satisfies `AICombatEvaluator.IsUnfavorableTrade`, the candidate is discarded (nulled) before the face fallback — a safety-net guard, still unreachable dead code today for the same reason as before (see that row's original note), retained as insurance for step 5 (board-state reasoning). `Combat.TryAttack`'s `failReason` is now logged via `Debug.Log` on failure instead of discarded (`out _`). The AI owns its *heuristic*; the *legality* of its pick is validated by `Combat.TryAttack`. |
| `AICombatEvaluator.cs` | `Scripts/AI/` | **AI plan step 1 of 5** (original), **extended step 2 of 5 (this session).** Static, stateless, mirrors `Combat.cs`'s pattern. `IsFavorableTrade(Minion attacker, Minion target)` returns true only if the attacker kills (`attacker.CurrentAttack >= target.CurrentHealth`) **and** survives (`target.CurrentAttack < attacker.CurrentHealth`); null-guards both arguments. `IsUnfavorableTrade(Minion attacker, Minion target)` (new, this session) is the mirror case: true if the attacker dies (`target.CurrentAttack >= attacker.CurrentHealth`) without killing the target (`attacker.CurrentAttack < target.CurrentHealth`); also null-guarded. The two predicates are mutually exclusive for any given pair. Pure evaluation only — no board/Taunt awareness, that stays `AIController`'s job (see that row). `IsFavorableTrade` playtest-confirmed (step 1). `IsUnfavorableTrade` is compile-verified and regression-verified (step 1's test replayed unchanged) but not playtested as new behavior — see Verification Status for why. |

## Card Pool
15 `CardData` assets in `EffectTester.cardPool` × `copiesPerCard = 2` = a 30-card deck: 12 Minions and 3 Spells. The Coin sits outside the pool.

| Minion | Cost | A/H | Note |
|---|---|---|---|
| Murloc | 1 | 1/1 | |
| Goblin | 2 | 2/2 | only card with artwork |
| Watchman | 2 | 3/1 | |
| Wisp | 2 | 1/1 | |
| River Crocodile | 3 | 2/3 | |
| Shieldbearer | 3 | 1/5 | **only Taunt** (`hasTaunt: 1`) |
| Warhorse | 3 | 3/3 | |
| Bear | 4 | 3/6 | |
| Boulderfist | 5 | 4/4 | |
| Charging Rhino | 6 | 5/5 | |
| Stone Guardian | 6 | 4/8 | |
| Ancient Colossus | 7 | 7/7 | |

Spells:

| Spell | Cost | `onPlayEffect` | `targetRequirement` |
|---|---|---|---|
| Fireball | 4 | `Effect_Deal3Damage` | Any |
| Arcane Bolt | 3 | `Effect_Deal3Damage` | Any |
| Mage Pupil | 4 | `Effect_GainMana1` | Self |
| The Coin | 0 | `Effect_GainMana1` | Self — **not in the pool**, granted via `EffectTester.coinCard` |

`Effect_Deal3Damage` is a `DealDamageEffect` (damage 3); `Effect_GainMana1` a `GainManaEffect` (mana 1).

**`targetsSelf: bool` → `TargetRequirement` enum (session 31).** Not a Constraint-12-style silent flip: the field was renamed, not just added, so Unity doesn't fall back to a C# initializer for existing assets — it just drops the old serialized `targetsSelf` key. All four spell assets (plus every other `CardData` asset that still had a stray `targetsSelf: 0` from Constraint 12's re-serialization hazard) were hand-edited in this session to carry the new `targetRequirement` key directly in their YAML, so nothing here is relying on a default fallback.

## Verification Status
- **Confirmed in play, still valid**: rendering both sides, decks/shuffle/draw, opening hands + Coin, mulligan both sides, lethal-via-spell ending the game with input genuinely locked out, `ConfirmMulliganButton` not lingering, background/music. Per-session detail in HISTORY.
- ✅ **Confirmed via playtest (Next Steps 1a–1d)**: Hero Power under manual control mode (correct player charged/flagged/damaged, regression-checked with manual control off), Taunt rejection (message, attacker stays selected), the wider attack path (Taunt resolves, AI-side Taunt enforcement and re-targeting after a kill, AI attack phase generally), and the visual render check (name/cost/stats on every card and minion). The own-side rejection remains unreachable from the UI and stays unverified.
- ✅ **Confirmed via playtest (session 30)**: spell-cast animation's travel-effect + target-reaction vertical slice — playing Fireball/Arcane Bolt against the opponent's face shows the projectile traveling from card to face and a red flash on impact. Lift/zoom and discard-off not built yet (Next Steps).
- ✅ **Confirmed via playtest (session 31) — targeting system, Next Steps 2, fully playtest-confirmed not just compile-verified**: exactly-lethal minion hit (no crash — Constraint 16's fix holds); non-lethal minion hit (target's idle sway visibly continues through the travel animation, confirming the null-check-and-fallback approach over an unconditional snapshot); exactly-lethal face hit (clean win, no crash, confirming `FaceView` was never reachable by the Constraint 16 bug class); own-minion targeting (Player Two self-killed their own Shieldbearer with Arcane Bolt — no side restriction, dead-minion cleanup handles a spell-killed friendly correctly); duplicate-card cancel/reselect (clicking the second copy of a pending card moves the highlight rather than cancelling — Constraint 17); self/none-target spells (Mage Pupil plays instantly, no pending-state UI); manual control mode (Player Two cast a targeted spell through the same flow); console clean, no leftover debug logging.
- ✅ **Confirmed via playtest (session 32) — drag-and-drop, Next Steps 4, fully playtest-confirmed not just compile-verified**: wrong-turn drag (no ghost spawns, gating confirmed at initiation, not just resolution); Player Two with manual control off on their own turn (no ghost — confirms the mode flag is checked independently of whose turn it is); Player Two with manual control on (ghost appears, drag/drop resolves normally); minion onto friendly board (summons); minion onto enemy board (rejected via the invalid-zone log, no crash); targeted spell onto a minion, including an exactly-lethal hit (Fireball, Player One) — re-confirms Constraint 16's fix specifically through the drag path, not just the old click path; targeted spell onto a face, both a miss (rejected) and a non-lethal hit (Player Two); self-target spell (Mage Pupil) resolving instantly on drop; console scan for leftover debug logging, zero results. Click-to-attack (`selectedAttacker`/`OnMinionClicked`/`OnFaceClicked`) untouched and unaffected by any of this.
- ✅ **Confirmed via playtest (session 33) — `MulliganController` extraction, Next Steps 16 step 1 of 4, fully playtest-confirmed not just compile-verified**: multiple cards mulliganed correctly, replacements drawn, mulligan panel and confirm button both hidden after confirm, turn-announcement log fired correctly immediately after confirming (the specific behavior worth checking given the callback consolidation onto `EffectTester.OnMulliganComplete`). No issues found.
- ✅ **Confirmed via playtest (session 33) — `CombatInputController` extraction, Next Steps 16 step 2 of 4, fully playtest-confirmed not just compile-verified**: select/deselect (green highlight); Taunt rejection working both directions; retarget after a rejected attack (attacker stayed selected, succeeded on retry); direct face attacks (Player One); AI-side attacks (Player Two's minions correctly attacking Player One's face on the AI's own turn with manual control off). No issues found.
- ✅ **Confirmed via playtest (session 33) — `CardDragResolver` extraction, Next Steps 16 step 3 of 4, fully playtest-confirmed not just compile-verified**: wrong-turn drag (no ghost); Player Two gating with manual control off (no ghost) and on (ghost appears, resolves correctly); minion onto friendly board (summons, confirmed both players); minion onto enemy board (rejected, no crash, confirmed both players with Murloc and Watchman); targeted spell onto a minion, exactly-lethal hit (no crash — highest-risk item, re-confirms the destroyed-`Transform` fix from Constraint 16 survived the extraction cleanly); targeted spell onto a face via Arcane Bolt (damage applied, animation played correctly); self-target spell (instant resolution); console scan for leftover `DragDiag` logging (zero results). No issues found.
- ✅ **Confirmed via playtest (session 33) — `GameManager` extraction, Next Steps 16 step 4 of 4, fully playtest-confirmed not just compile-verified**: Hero Power for Player One (mana/cooldown correctly gated, damage applied to Player Two, second click same turn correctly blocked); Hero Power for Player Two under manual control (confirmed the six-hardcoded-reference bug class from the session 31 targeting work did **not** resurface — Player Two's own mana/cooldown charged, damage applied to Player One, same-turn cooldown blocked correctly); turn sequencing (End Turn → full AI turn → correct return to Player One, right turn number, mana refresh); win condition + lockout (a combat kill on Player Two's hero fires the win message and locks out **all** input — Hero Power, End Turn, and drag — afterward); full regression, fresh game start to game end, mulligan → combat → drag all working end to end. No issues found. **This completes playtest confirmation for all four `EffectTester` decomposition components — see Next Steps 16.**
- ✅ **Confirmed via playtest (this session) — mulligan batching fix (Next Steps 6)**: mulliganing the entire opening hand (3 cards) produced a replacement hand with zero overlap against the set-aside cards. A second test mulliganing a single Watchman drew another Watchman — investigated and confirmed a false alarm (the deck's untouched second copy, not the returned card; see Known Issues note under the `CardData` reference-identity entry). Correctness closed out on code reasoning rather than further probabilistic testing: `MulliganCards` removes every set-aside card from `Hand` before any `DrawCard()` call runs and only re-adds them to `Deck` afterward, so it is structurally impossible for a draw inside that method to return a card still in the removed list, regardless of card-name duplicates.
- **Never verified at all**: the own-side rejection (unreachable from the UI today — see Next Steps 1c note; drag-and-drop only replaced card-play, not the attack flow, so this is still unreached). **Deliberately accepted as still-open through the `CombatInputController` extraction (session 33, Next Steps 16, playtest-confirmed above)** — the gap is about UI reachability, which moving the code to a new class doesn't change either way; not fixed as part of that split.
- ✅ **Confirmed via playtest (this session) — `AICombatEvaluator` + favorable-trade wiring, AI plan step 1 of 5**: staged via the manual-control-toggle trick (enable Manual Control Mode to hand-place minions on both boards past summoning sickness — see `EffectTester`'s `manualControlMode` field — then uncheck it right before End Turn so `GameManager.OnEndTurnClicked` re-arms `AIController.TakeTurn()` against the manually-staged board instead of skipping it). Board: AI's Warhorse vs. Player One's Murloc, no Taunt on either side. AI attacked the Murloc (favorable trade — kills, survives) instead of defaulting to face. Confirms the Taunt pre-filter still gates correctly (only consulted when `board.GetTauntMinions(opponent)` is empty) and that `AICombatEvaluator.IsFavorableTrade` drives the pick as designed. **Steps 3–5 of the AI plan remain for future sessions**: a Taunt-pre-filter regression check under the new logic, lethal-detection override (attack face when lethal is available regardless of trades), and board-state-aware spell targeting — none built yet.
- ✅ **Compile-verified and regression-verified (this session) — `AICombatEvaluator.IsUnfavorableTrade` + guard wiring, AI plan step 2 of 5**: not playtested as new-behavior demonstration, because the guard cannot currently fire — `IsFavorableTrade` and `IsUnfavorableTrade` are mutually exclusive on the same attacker/target pair, and the attack loop only ever proposes a candidate via `IsFavorableTrade`, so there is no board state today where the guard's null-out branch executes. In place of a new-behavior playtest, replayed step 1's exact test (Charging Rhino vs. Murloc, favorable trade, manual-control-toggle staging) to confirm no regression: Charging Rhino still killed the Murloc correctly. The guard is deliberate insurance for step 4 (lethal override) and step 5 (board-state reasoning), which may later select attack candidates through logic other than the `IsFavorableTrade` scan.
- ✅ **Confirmed via playtest (2026-08-27) — Taunt pre-filter regression check under the new trade logic, AI plan step 3 of 5**: no new code, staged via the same manual-control-toggle trick. Board: AI's Charging Rhino (5/5) vs. Player One's Shieldbearer (1/5, Taunt) and Murloc (favorable-trade bait) both present. AI attacked the Shieldbearer (killed, Taunt honored) instead of taking the tempting favorable trade against the Murloc; Charging Rhino survived at 5/4, Murloc untouched. Confirms the Taunt check still wins over trade evaluation with a real favorable-trade candidate on the board, not just an empty one.
- ✅ **Compile-verified (2026-08-27) — lethal-detection override, AI plan step 4 of 5**: `dotnet build AI.csproj` clean, 0 errors/warnings. Computed once per turn ahead of the attack loop, gated on `board.GetTauntMinions(opponent).Count == 0` so a Taunt always still forces the Taunt attack regardless of raw unblocked-damage math (Taunt and lethal cannot coexist by construction — verified by code reading, not yet by a dedicated playtest scenario). **Not yet playtest-confirmed.** A playtest attempt this session (Scenario 4 from the original plan — give the AI two or more attackers whose summed `CurrentAttack`, only those with `CanAttack`, is >= the opponent's current `Health`, with no Taunt on the opponent's board; expected every attacker swings face for lethal, none diverted into a favorable trade first even with a killable enemy minion present) did not produce a clean run — repeated manual-control staging errors (cards dragged onto the wrong board) burned the remaining session time before a valid board state was reached. Scenario 4 still needs to be run cleanly in a future session before step 4 can be marked confirmed. **Step 5 (board-state-aware spell targeting) remains for a future session.**
- **Compile-verified**: all five assemblies build clean via `dotnet build Core.csproj` (and `Effects`, `Cards`, `AI`, `UI`), no Unity launch needed. **Catches syntax, not behaviour.**

## Known Issues — real, not fixed
- ✅ **RESOLVED (this session, Next Steps 6) — Mulligan could hand back the card you just returned.** The old per-card `MulliganCard()` did remove → add-to-deck → shuffle → draw *per card*, so a card mulliganed early in a multi-card mulligan could be shuffled back in before later draws in the same batch. Replaced with `PlayerHand.MulliganCards(List<CardData>)`: removes **all** selected cards from `Hand` first, draws that many replacements from the deck as it stood (excluding every set-aside card), and only *then* adds the set-aside cards back to `Deck` and shuffles once — matching real Hearthstone's batching. `MulliganController.OnConfirmMulliganClicked` and `AIController.PerformMulligan` both updated to collect their full selection and call `MulliganCards` once instead of looping. **Approved deliberate behaviour change**: because set-aside cards are no longer temporarily re-added to the deck before each draw, mulliganing more cards than remain in the deck can trigger fatigue where the old code wouldn't (only reachable with a near-empty deck, not a realistic 30-card game start) — this is correct "draw before shuffle-back" behaviour, not a regression.
- **`FaceView` idle-animation base capture can land pre-layout.** Only `OpponentAvatarImage` is exposed (stretch-anchored under a canvas-stretched parent); `AvatarImage` is point-anchored and safe. **Cannot fire on the normal path** — the only trigger is `ShowMulliganUI()`'s unwired-panel fallback, which calls `RefreshAll()` from inside `Start()`. Full analysis and the exact anchor values in HISTORY.
- ✅ **RESOLVED (this session)**: ~~Both avatar `Image`s still have `Raycast Target` checked~~ Craig unchecked `Raycast Target` on both `AvatarImage` (`PlayerFaceDisplay`) and `OpponentAvatarImage` (`OpponentFaceDisplay`) directly in the Unity Editor, per the standing habit that decorative Images shouldn't block clicks — confirmed via scene diff, exactly those two `m_RaycastTarget: 1 → 0` changes, nothing else. The two avatars still use inconsistent anchor types — that part remains open (Next Steps 10).
- ✅ **RESOLVED (session 31, Next Steps 2)**: ~~Spells can only ever target the enemy face.~~ A spell can hit either face or any minion, either side. Originally via click-a-card-then-click-a-target; that flow was fully replaced by drag-and-drop in session 32 (Next Steps 4) — same targeting rules, resolved in `EffectTester.ResolveCardDrag` instead. See Code Written So Far and Constraints 15, 16, 18.
- **No draw-game handling.** `CheckWinCondition()` checks Player One first, so a simultaneous double-KO always awards Player Two the win.
- **`gameOver`/`winner` are never reset** — no restart/new-game path exists.
- **`CardData` reference identity is not clean** (full chain in HISTORY): mulligan-selecting one copy of a duplicate toggles the shared entry. `Hand.Contains`/`Remove` and the mulligan `HashSet<CardData>` are still blind to this — that root cause is still open. **Session 31 added a second, narrower instance of the same blindness** (the spell-targeting pending-selection state) and fixed *that one* with a scoped workaround — tracking by hand-index instead of by `CardData` reference (Constraint 17) — not a root-cause fix. Fixing the underlying identity problem (giving `Hand` entries real per-copy identity) is a bigger change touching `PlayerHand`, mulligan, and more; it needs its own planning pass, not another one-off workaround grafted on per-feature. **Playtest note (this session, mulligan-batching fix)**: mulliganing a single Watchman (deck holds 2 copies) returned a hand containing another Watchman — investigated and confirmed **not** a regression: only one copy was set aside, the second was already sitting untouched in the deck the whole time, and drawing it is valid and expected. Given this known reference-identity gap, **the same card name reappearing after a mulligan is not by itself evidence of anything** — it cannot be visually distinguished from "the untouched sibling copy was drawn." A future session seeing this symptom should not read it as evidence the batching fix regressed; correctness here rests on the code proof (see resolved entry above: `MulliganCards` structurally cannot draw a card still in its own removed list), not on eyeballing card names post-mulligan.
- **Fatigue and the 7-minion board cap compile but have never been triggered** in play.
- **Manual control mode's turn-gating fix is written but was never re-tested.**
- Smaller: 0-attack minions can attack; `GameContext.Board` and `CardData.description` declared but never read; `TurnNumber` counts half-turns; `Target.GainMana` is uncapped, so The Coin can exceed both `MaxMana` and 10; `OnFaceClicked` blocks attacking your own face as a UI guard, in addition to Combat's own-side rejection.
- **Cosmetic**: `BoardPanel`/`OpponentBoardPanel` text clipping (minion name cut off at the left edge); board panel vertical spacing needs more separation; `MulliganPanel` shares `HandPanel`'s screen position (fine, hidden after confirm).

## Next Steps (in order)
1. ✅ **Playtest — confirmed, all four passes.**
   - **1a — Hero Power under manual control mode**: confirmed. As Player Two, the *correct player* is charged 2 mana, flagged `HasUsedHeroPowerThisTurn`, and takes the 1 damage; regression-checked identical with manual control off.
   - **1b — Taunt rejection**: confirmed. With a Taunt up, attacking a non-Taunt target is rejected, the message names the owner, and the attacker stays selected.
   - **1c — The wider attack path**: confirmed. Attacking the Taunt minion itself resolves; AI-side Taunt enforcement and re-targeting after killing a Taunt work; AI attack phase generally works. The own-side rejection **still cannot be reached from the UI** — own-minion clicks route to selection and own-face clicks are blocked in `OnFaceClicked` — so it remains unverified. Neither targeting (Next Steps 2) nor drag-and-drop (Next Steps 4) touched the attack flow, so this is still open; reaching it needs a change to `OnMinionClicked`/`OnFaceClicked`/`Combat.TryAttack` itself, not another card-play feature layered on top.
   - **1d — Visual render check**: confirmed. Every card in hand and every minion on both boards shows name, cost and stats. `MinionView`'s `nameText`/`statsText` originally overflowed the card (box wider than the card, no word wrap, and the card's `RectTransform` didn't match the `LayoutElement` size the layout group actually rendered at) — fixed session 29: word wrap + TMP auto-sizing enabled, text boxes narrowed to fit, and the card resized to 170x190 (`RectTransform` and `LayoutElement` matched each other). Session 30 resized again to 160x220 to match `CardView`'s root exactly, and fixed a follow-on bug where artwork-bearing minions appeared to render larger than their neighbours (Constraint 14).
2. ✅ **Targeting system — DONE, playtest-confirmed (session 31).** Spells could target a minion (either side) or either face via click-a-card-then-click-a-target, not just the enemy face — since superseded by drag-and-drop (Next Steps 4), which replaced the click flow entirely. Built on: `CardData.targetRequirement` (`TargetRequirement { None, Self, Any }`, replacing `targetsSelf: bool`, still in use). Two bugs found and fixed during the build: a lethal-hit `Transform`-destroyed crash (Constraint 16, still relevant — re-confirmed through the drag path in session 32) and a duplicate-card pending-selection identity quirk (old Constraint 17, since removed along with the click flow it described).
3. **More spells.** Content on the existing `CardEffect` system, but AoE / heal / buff / draw each need a new `CardEffect` subclass. Scope first: pick the spells, then sort into "new asset only" vs "needs a new effect class".
4. ✅ **Drag-and-drop to play cards — DONE, playtest-confirmed (session 32).** Replaces click-to-play (and the click-then-click targeting flow from Next Steps 2) entirely — dragging a card is now the only way to play one; click-to-attack is untouched. Built on: `CardView` implementing `IBeginDragHandler`/`IDragHandler`/`IEndDragHandler` directly, a ghost cloned under the root `Canvas`, `EffectTester.ResolveCardDrag` centralising drop resolution via `EventSystem.RaycastAll` (minion → friendly board only, `Any`-spell → `MinionView`/`FaceView` hit, `None`/`Self` → any recognized zone), and `CanPlayerOneDrag`/`CanPlayerTwoDrag` as the single gating source for both drag initiation and drop resolution. One real bug found and fixed: `boardPanel`/`opponentBoardPanel` had no raycastable `Graphic`, so empty board space returned zero raycast hits (Constraint 18). Full checklist in Verification Status above.
5. Generate art for the remaining 14 cards (only Goblin has art). Pure content.
6. ✅ **Batch the mulligan — DONE (this session).** `PlayerHand.MulliganCards(List<CardData>)` replaces the per-card `MulliganCard()`; see resolved entry in Known Issues for the mechanism and the approved fatigue-edge-case behaviour change.
7. Longer playtest to trigger fatigue and the 7-minion board cap.
8. Check the same minion attacking twice in one turn is rejected.
9. Re-test manual control mode's turn-gating fix.
10. `FaceView` scene issues: `OpponentAvatarImage`'s pre-layout exposure, inconsistent anchors. (Both avatars' `Raycast Target` — done, this session, see Known Issues.)
11. More board backgrounds and/or music tracks (currently 3 backgrounds, 1 track).
12. **Smarter AI attack logic — 3 of 5 done, step 4 built but not yet playtest-confirmed.** (1) `AICombatEvaluator.IsFavorableTrade` + wiring into `AIController`'s attack loop, consulted only when no enemy Taunt exists — playtest-confirmed. (2) `AICombatEvaluator.IsUnfavorableTrade` + safety-net guard wiring — compile-verified and regression-verified; currently unreachable dead code by design (see Code Written So Far and Verification Status for why), kept as insurance for step 5. (3) Taunt-pre-filter regression check under the new logic — playtest-confirmed (2026-08-27), no new code: AI honored Taunt over a live favorable-trade alternative. (4) Lethal-detection override — built and compile-verified (2026-08-27): computed once per turn, gated on no enemy Taunt, attacks face with every available attacker when total unblocked damage is lethal, bypassing trade evaluation entirely; **not yet playtest-confirmed** — a Scenario 4 attempt this session was cut short by repeated manual-control staging errors (see Verification Status). Remaining: (5) board-state-aware spell targeting.
13. Draw-game outcome and a restart/new-game path.
14. **Tier 2 content**: Deathrattle, Charge/Rush, Divine Shield, Windfury, Silence, full Hero classes + real Hero Powers (the current one is a placeholder), Weapons — each needs its own scoping pass. ⚠️ **Sequencing — see Live Constraint 12.** Deathrattle/Charge/Divine Shield/Windfury/Silence is five new bools on `CardData`, and all 16 existing assets will physically omit all five, falling back to the C# initializer. Add them with `false` initializers, force re-serialize every `CardData` asset so the fields are written into the files, and only then start setting per-card flags. Cards created after the fields exist are unaffected.
15. Record `BoardPanel`'s corrected `Pos Y` and `HandPanel`'s Rect Transform values.
16. ✅ **`EffectTester` decomposition — DONE, all 4 components extracted, compile-verified, and playtest-confirmed (session 33).** Supersedes the old "delete/rename to `GameBootstrapper`" note — see Constraint 11, now marked resolved with the reasoning for why a straight rename was superseded by this split. All four target components landed: `MulliganController` ✅ extracted and playtest-confirmed, `CombatInputController` ✅ extracted and playtest-confirmed — own-side-rejection gap deliberately carried through unfixed (see that row in Code Written So Far and Verification Status), `CardDragResolver` ✅ extracted and playtest-confirmed — not a "finished" component regardless, since Next Steps 17's lift/zoom/discard-off work will touch it again once the Constraint 15 decision is made, `GameManager` ✅ extracted and playtest-confirmed (full checklist: Hero Power both players, turn sequencing through a full AI turn, win condition + total input lockout, full mulligan→combat→drag regression — see Verification Status). View refresh (`RefreshAll`/`RefreshHandDisplay`/`RefreshBoardDisplay`/`RefreshFaceDisplay`) stays on `EffectTester` itself, not extracted as a fifth component — it depends on all the others' public surfaces. Shared model state passed by explicit constructor reference; cross-cutting gates (`gameOver`, `mulliganController.MulliganComplete`, `manualControlMode`) injected as `Func<bool>` predicates, extending the `CanPlayerOneDrag`/`CanPlayerTwoDrag` pattern; `AfterGameAction` becomes an injected `Action` callback rather than sibling method calls. Incremental: one component at a time, compile-verify + playtest after each. See Constraint 19 for a cost that recurs across every extraction.
17. **Spell-cast animation, remainder**: card lift/zoom on play, and the spent card animating off to discard. Travel effect + target reaction shipped session 30 (`SpellAnimationSequencer`), extended to minion targets and the lethal-hit edge case in session 31 (Constraint 16). Both remaining pieces need the same problem solved first — `HandDisplay.RenderHand` destroys the played `CardView` synchronously before any animation on it could finish (Constraint 15) — and that decision is still open: either defer `RefreshHandDisplay` until the sequence completes, or have `RenderHand` skip whichever card is currently mid-animation. Discard-off additionally needs a discard-pile anchor point, which doesn't exist in the scene yet.

## Git Habits
- `git add .` / `git commit -m "short summary"` / `git push`, pushed regularly to `github.com/craig-middleton/hearthClone`.
- `.gitignore` excludes `Library/`, `Temp/`, `.sln`/`.slnx`, `.csproj`.
